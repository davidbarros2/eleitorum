[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "eleitorum"
version = "1.0.0"
description = "Windows desktop utility to normalize electoral roll and eligibility list files"
readme = "README.md"
requires-python = ">=3.11,<3.14"
license = { text = "MIT" }
dependencies = [
    "openpyxl==3.1.5",
    "xlrd==2.0.2",
    "odfpy==1.4.1",
    "pandas==3.0.3",
    "charset-normalizer==3.4.7",
    "PySide6==6.11.1",
]

[project.optional-dependencies]
dev = [
    "pytest==9.0.3",
    "pytest-cov==7.1.0",
    "mypy==2.1.0",
    "ruff==0.15.14",
    "pytest-qt==4.5.0",
    "pyinstaller==6.20.0",
    "svglib==1.6.0",
    "reportlab==4.4.10",
    "Pillow==12.1.1",
    "pip-audit==2.10.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "N", "SIM"]
ignore = ["N802"]  # Qt virtual method overrides are required to be camelCase

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["E501", "E741", "F841"]  # test files: long assert messages, l/i var names, unused captures

[tool.mypy]
python_version = "3.11"
warn_unused_ignores = true

[[tool.mypy.overrides]]
module = "eleitorum.core.*"
disallow_untyped_defs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-x -q --strict-markers"
qt_api = "pyside6"
markers = [
    "integration: full pipeline tests (slower)",
    "performance: 150k-row benchmark tests",
]

[tool.coverage.run]
source = ["src/eleitorum/core"]

[tool.coverage.report]
fail_under = 90
show_missing = true
