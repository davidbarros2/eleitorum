---
phase: quick-260525-q6z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eleitorum/ui/steps/step_type.py
  - src/eleitorum/ui/steps/step_upload.py
  - src/eleitorum/ui/steps/step_sheet.py
  - src/eleitorum/ui/wizard.py
autonomous: true
requirements: [WIZ-01, WIZ-02, WIZ-03]

must_haves:
  truths:
    - "Clicking a card on StepType immediately enables the Próximo button"
    - "Choosing a valid file on StepUpload immediately enables the Próximo button"
    - "Selecting a sheet on StepSheet immediately enables the Próximo button"
    - "Próximo returns to disabled if selection is cleared (StepSheet deselect)"
  artifacts:
    - path: "src/eleitorum/ui/steps/step_type.py"
      provides: "completion_changed Signal + emit in _on_selection"
      contains: "completion_changed = Signal()"
    - path: "src/eleitorum/ui/steps/step_upload.py"
      provides: "completion_changed Signal + emit in _on_file_received"
      contains: "completion_changed = Signal()"
    - path: "src/eleitorum/ui/steps/step_sheet.py"
      provides: "completion_changed Signal + emit in _on_selection_changed"
      contains: "completion_changed = Signal()"
    - path: "src/eleitorum/ui/wizard.py"
      provides: "Three completion_changed → _update_navbar_for_current_step connections"
      contains: "completion_changed.connect"
  key_links:
    - from: "src/eleitorum/ui/steps/step_type.py"
      to: "src/eleitorum/ui/wizard.py"
      via: "completion_changed signal → _update_navbar_for_current_step slot"
      pattern: "_step_type\\.completion_changed\\.connect"
    - from: "src/eleitorum/ui/steps/step_upload.py"
      to: "src/eleitorum/ui/wizard.py"
      via: "completion_changed signal → _update_navbar_for_current_step slot"
      pattern: "_step_upload\\.completion_changed\\.connect"
    - from: "src/eleitorum/ui/steps/step_sheet.py"
      to: "src/eleitorum/ui/wizard.py"
      via: "completion_changed signal → _update_navbar_for_current_step slot"
      pattern: "_step_sheet\\.completion_changed\\.connect"
---

<objective>
Fix the "Próximo" button never enabling after user interaction on steps 1–3.

Purpose: _update_navbar_for_current_step() is called once on navigation but never
re-called when the user makes a selection. Adding a completion_changed Signal to
each of the three affected step widgets and wiring it to _update_navbar_for_current_step
in WizardController closes the reactivity gap.

Output: Four modified files; Próximo button responds immediately to user
selections on StepType, StepUpload, and StepSheet.
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/david/Documents/Projetos/eleitorum/.planning/STATE.md
@C:/Users/david/Documents/Projetos/eleitorum/src/eleitorum/ui/wizard.py
@C:/Users/david/Documents/Projetos/eleitorum/src/eleitorum/ui/steps/step_type.py
@C:/Users/david/Documents/Projetos/eleitorum/src/eleitorum/ui/steps/step_upload.py
@C:/Users/david/Documents/Projetos/eleitorum/src/eleitorum/ui/steps/step_sheet.py

<interfaces>
<!-- Existing signal pattern in wizard.py for reference -->
<!-- quit_requested = Signal()  # line 76 — same pattern to follow -->

<!-- _update_navbar_for_current_step() signature (line 404 of wizard.py): -->
<!--   def _update_navbar_for_current_step(self) -> None: -->
<!--   Polls self._stack.currentWidget().is_complete() to set Próximo enabled. -->
<!--   Called on every navigation; must also be called whenever a step widget -->
<!--   changes its completion state. -->

<!-- Signal import already present in all three step files via PySide6.QtWidgets -->
<!-- imports block. Signal must be imported from PySide6.QtCore — check each file. -->
<!-- step_type.py line 13: from PySide6.QtWidgets import ... (no QtCore import) -->
<!-- step_upload.py line 17: from PySide6.QtWidgets import ... (no QtCore import) -->
<!-- step_sheet.py line 17: from PySide6.QtCore import Qt  — Signal not yet imported -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add completion_changed Signal to step_type, step_upload, step_sheet</name>
  <files>
    src/eleitorum/ui/steps/step_type.py,
    src/eleitorum/ui/steps/step_upload.py,
    src/eleitorum/ui/steps/step_sheet.py
  </files>
  <behavior>
    - Test 1 (StepType): After _on_selection("caderno"), completion_changed was emitted exactly once.
    - Test 2 (StepType): After _on_selection("elegiveis"), completion_changed was emitted exactly once.
    - Test 3 (StepUpload): After _on_file_received() accepts a valid path, completion_changed was emitted.
    - Test 4 (StepUpload): After _on_file_received() rejects an invalid extension (session.source_path stays None), completion_changed is NOT emitted (the return-early path has no state change to broadcast).
    - Test 5 (StepUpload): After _on_file_received() triggers an EleitorumError and clears source_path, completion_changed IS emitted (state changed from Some → None).
    - Test 6 (StepSheet): After _on_selection_changed(current=item, previous=None), completion_changed was emitted.
    - Test 7 (StepSheet): After _on_selection_changed(current=None, previous=item), completion_changed was emitted (deselect also triggers re-poll).
  </behavior>
  <action>
    For each of the three step files:

    step_type.py:
    - Add "from PySide6.QtCore import Signal" to the imports (QtWidgets import stays).
    - Add class attribute "completion_changed = Signal()" on the line immediately after the class docstring, before __init__.
    - In _on_selection(self, key): append "self.completion_changed.emit()" as the last line of the method (after the if/else that deselects the other card).

    step_upload.py:
    - Add "from PySide6.QtCore import Signal" to the imports.
    - Add class attribute "completion_changed = Signal()" after the class docstring, before __init__.
    - In _on_file_received(self, path_str): the method has two exit paths after the extension whitelist check:
        a) Invalid extension path: ends with "return" at line 118 — do NOT emit here (no session state changed).
        b) Success path (valid extension, list_sheets succeeds): emit "self.completion_changed.emit()" after "self._session.sheets = sheets" (last line of the try block).
        c) EleitorumError path (list_sheets raises): emit "self.completion_changed.emit()" after "self._file_name_label.setText("")" (after clearing source_path).

    step_sheet.py:
    - "from PySide6.QtCore import Qt" is already present (line 17). Extend it: "from PySide6.QtCore import Qt, Signal".
    - Add class attribute "completion_changed = Signal()" after the class docstring, before __init__.
    - In _on_selection_changed(self, current, previous): append "self.completion_changed.emit()" as the last line of the method (after the if/else that writes session.sheet_name). Emit regardless of whether current is None or not — both branches change completion state.
  </action>
  <verify>
    <automated>python -m pytest tests/ui/test_step_signals.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    All seven signal-emission tests pass. completion_changed is declared on all three
    classes and emitted at the correct points in their slot methods.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire completion_changed signals to _update_navbar_for_current_step in WizardController</name>
  <files>src/eleitorum/ui/wizard.py</files>
  <action>
    In WizardController.__init__, locate the block that ends with:

        navbar.cancelar_clicked.connect(self.on_cancelar)

    Immediately after that line, add three new connections:

        self._step_type.completion_changed.connect(self._update_navbar_for_current_step)
        self._step_upload.completion_changed.connect(self._update_navbar_for_current_step)
        self._step_sheet.completion_changed.connect(self._update_navbar_for_current_step)

    No other changes to wizard.py. The stale comment block at lines 431–436
    (beginning with "# Connect/disconnect proximo to navbar...") describes intent
    but was never implemented — remove it now that the implementation exists, to
    avoid misleading future readers.
  </action>
  <verify>
    <automated>python -m pytest tests/ui/test_wizard_navbar.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    WizardController connects all three completion_changed signals. Existing navbar
    tests pass. The stale comment is removed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Signal→Slot | Internal Qt signal/slot — no external input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q6z-01 | Tampering | Signal connections in __init__ | accept | Pure internal wiring; no user-supplied data; slots are private methods of the same object |
</threat_model>

<verification>
After both tasks complete, perform an end-to-end smoke check:

1. Launch the app: python -m eleitorum (or run the EXE).
2. Step 1 (StepType): Próximo must be disabled on arrival; click "Caderno Eleitoral" → Próximo must enable immediately without navigating away.
3. Step 2 (StepUpload): Navigate to step 2; Próximo must be disabled; choose a valid file → Próximo must enable immediately.
4. Step 2.5 (StepSheet): Open a multi-sheet Excel file; navigate to sheet step; Próximo must be disabled; click any sheet → Próximo must enable immediately.
5. Confirm StepColumns still auto-enables Próximo on arrival (is_complete() always True) — this step is unaffected.

Automated regression:
    python -m pytest tests/ui/ -x -q
</verification>

<success_criteria>
- completion_changed Signal declared and emitted in step_type.py, step_upload.py, step_sheet.py.
- WizardController wires all three signals to _update_navbar_for_current_step immediately after the NavBar signal block.
- Stale comment at wizard.py lines 431–436 removed.
- All existing tests pass: python -m pytest -x -q.
- Próximo button enables reactively on user interaction for all three steps.
</success_criteria>

<output>
Create `.planning/quick/260525-q6z-fix-proximo-button-not-enabling-on-step-/260525-q6z-SUMMARY.md` when done.
</output>
