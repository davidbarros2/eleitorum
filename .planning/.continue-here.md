---
context: default
phase: null
task: null
total_tasks: null
status: paused
last_updated: 2026-05-24T09:26:51Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints from active failure. All 4 development phases are complete._

<current_state>
All four development phases (core pipeline, UI, integration tests, build/CI/distribution) are complete and verified. This pause follows a post-milestone session that produced:
- `GUIA_DE_TESTES.md` — a comprehensive PT-PT end-to-end testing guide for a non-technical (layman) tester
- `README.md` updated to add a link to the testing guide

These changes are uncommitted. The v1.0.0 tag has NOT yet been pushed — no GitHub Release exists yet.
</current_state>

<completed_work>
- Task 1: Wrote `GUIA_DE_TESTES.md` — 14 tests (A–N) in PT-PT, covering all deferred manual checks from phases 2–4 plus additional functional tests. Includes step-by-step test data creation in Notepad, exact expected outputs, and a results table for feedback. ✓
- Task 2: Updated `README.md` — added "Guia de Testes" subsection under "Instalação" with link to `GUIA_DE_TESTES.md`. ✓
</completed_work>

<remaining_work>
- Task 3: Commit all pending files (`GUIA_DE_TESTES.md`, `README.md`, `HANDOFF.json`, `.continue-here.md`)
- Task 4: Push main branch to GitHub (triggers CI — ruff, mypy, pytest on Python 3.11 + 3.12)
- Task 5: Create and push `v1.0.0` tag → triggers `release.yml` → PyInstaller build on windows-latest → smoke test → GitHub Release with `EleitorUM-1.0.0-win64.zip` + `.sha256`

**Then (human actions):**
- David makes the repo public (GitHub Settings → Change visibility → Public)
- David tests from another workstation following `GUIA_DE_TESTES.md`
- David returns with test findings for final assessment
</remaining_work>

<decisions_made>
- Testing guide written in PT-PT: app is PT-PT; target users are Portuguese-speaking university staff; David can read PT-PT.
- Test data provided as inline Notepad copy-paste (not pre-built files): tester has only the EXE ZIP from GitHub Releases; Notepad approach is self-contained.
- 14 tests (A–N): covers all 7 deferred manual checks from phases 2–4 (updated check F: no UMinho disclaimer, shows v1.0.0) + 7 additional tests (unsupported format, validation errors, multi-sheet Excel, output format, --version CLI, GitHub release workflow).
</decisions_made>

<blockers>
- **CRITICAL — GitHub Release does not exist yet**: the `v1.0.0` tag has not been pushed. Without it, `release.yml` does not run, no EXE is built, and the tester cannot download anything. This tag MUST be pushed before making the repo public.
</blockers>

## Required Reading (in order)
1. `GUIA_DE_TESTES.md` — the testing guide just written; review it before pushing
2. `.planning/phases/04-build-ci-packaging-distribution/04-VERIFICATION.md` — lists the 4 human-verification items that `GUIA_DE_TESTES.md` covers
3. `C:\Users\david\.claude\projects\C--Users-david-Documents-Projetos-eleitorum\memory\deferred_manual_checks.md` — original deferred UI checks from Phase 2; all incorporated into the testing guide (check F updated to reflect v1.0.0 + no UMinho disclaimer)

## Critical Anti-Patterns (do NOT repeat these)
_None identified this session._

## Infrastructure State
- Git: branch `main`, clean except for 4 uncommitted files (see remaining_work)
- GitHub: repo is **private**, no tags, no GitHub Release yet
- CI: workflows in `.github/workflows/ci.yml` and `release.yml` are ready and verified
- Python env: `pip install -e ".[dev]"` installs all deps including PyInstaller

## Pre-Execution Critique Required
_Not applicable — no execution pending; commit/push is straightforward._

<context>
All development is done. The session goal was to prepare for external testing: write a layman-friendly guide, update the README, then push the v1.0.0 tag so GitHub Actions builds and publishes the Windows EXE. After David makes the repo public, a non-technical tester follows GUIA_DE_TESTES.md and returns with pass/fail results. If all tests pass, the project is closed. If any fail, targeted fixes are made.
</context>

<next_action>
Run these three commands in sequence:
1. `git add GUIA_DE_TESTES.md README.md .planning/HANDOFF.json .planning/.continue-here.md`
2. `git commit -m "docs: add layman testing guide and README link to GUIA_DE_TESTES.md"`
3. `git push origin main`
4. `git tag v1.0.0 && git push origin v1.0.0`  ← triggers release.yml (PyInstaller build + GitHub Release)
Then tell David: make the repo public, test from another workstation, return with findings.
</next_action>
