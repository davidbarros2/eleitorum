---
phase: "04-build-ci-packaging-distribution"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eleitorum/version.py
  - src/eleitorum/__main__.py
  - src/eleitorum/ui/strings.py
  - src/eleitorum/ui/dialogs.py
  - pyproject.toml
  - tests/unit/test_no_uminho_strings.py
autonomous: true
requirements:
  - BRAND-04
  - REPO-09

must_haves:
  truths:
    - "Running `python -m eleitorum --version` prints `EleitorUM 1.0.0` and exits with code 0 without opening a window"
    - "No file in src/, README.md, or pyproject.toml contains the string 'Universidade' or 'UMinho'"
    - "The version string 1.0.0 is defined only in version.py; all other consumers import from it"
    - "The test_no_uminho_strings.py regression test passes as part of the normal pytest run"
    - "pyproject.toml version field is 1.0.0 and description contains no institution name"
  artifacts:
    - path: "src/eleitorum/version.py"
      provides: "Canonical version string 1.0.0"
      contains: '__version__ = "1.0.0"'
    - path: "src/eleitorum/__main__.py"
      provides: "--version argparse before Qt imports"
      exports: ["_check_version_flag", "main"]
    - path: "src/eleitorum/ui/strings.py"
      provides: "ABOUT_DESCRIPTION without UMinho; UMINHO_DISCLAIMER removed; WELCOME_BODY without institution"
    - path: "src/eleitorum/ui/dialogs.py"
      provides: "AboutDialog without UMINHO_DISCLAIMER widget"
    - path: "tests/unit/test_no_uminho_strings.py"
      provides: "Regression guard scanning src/, README.md, pyproject.toml"
  key_links:
    - from: "src/eleitorum/__main__.py"
      to: "src/eleitorum/version.py"
      via: "import inside _check_version_flag() — safe before Qt"
      pattern: "from eleitorum.version import __version__"
    - from: "src/eleitorum/ui/dialogs.py"
      to: "src/eleitorum/ui/strings.py"
      via: "import block — UMINHO_DISCLAIMER must NOT appear"
      pattern: "UMINHO_DISCLAIMER"
---

<objective>
Bump the version to 1.0.0, add the `--version` CLI argument that exits before Qt, remove all Universidade do Minho / UMinho references from source files, and add a regression test that will fail CI if any reference re-appears.

Purpose: These changes are prerequisites for every downstream plan — the build script reads version.py, the CI smoke test invokes `--version`, and the grep test guards all future changes.

Output: Six modified/new files; `pytest` continues to pass with 383 + 1 new tests green.
</objective>

<execution_context>
@C:\Users\david\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\david\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\david\Documents\Projetos\eleitorum\.planning\ROADMAP.md
@C:\Users\david\Documents\Projetos\eleitorum\.planning\phases\04-build-ci-packaging-distribution\04-CONTEXT.md
@C:\Users\david\Documents\Projetos\eleitorum\.planning\phases\04-build-ci-packaging-distribution\04-PATTERNS.md

<interfaces>
From src/eleitorum/version.py (current — to be bumped):
  __version__ = "0.1.0"

From src/eleitorum/__main__.py (current — to be refactored):
  Line 14: from eleitorum.ui.app import create_app        # module-level Qt import — MOVE inside main()
  Line 15: from eleitorum.ui.main_window import MainWindow # module-level Qt import — MOVE inside main()

From src/eleitorum/ui/strings.py (current — UMinho targets):
  Line 166-168: ABOUT_DESCRIPTION contains "da Universidade do Minho" — update
  Lines 172-178: UMINHO_DISCLAIMER constant + comment above it — delete entirely
  Lines 185-193: WELCOME_BODY line 188 contains "plataforma da Universidade do Minho" — update to "plataforma eleitoral"

From src/eleitorum/ui/dialogs.py (current — UMinho targets):
  Line 37: UMINHO_DISCLAIMER in import list — remove
  Lines 122-125: disclaimer QLabel widget block — delete entirely
  Module docstring line 10-11: remove "UMinho disclaimer" mention

From pyproject.toml (current):
  Line 7: version = "0.1.0"  — bump to "1.0.0"
  Line 8: description = "...for Universidade do Minho"  — strip institution phrase
  Lines 22-28: dev extras — add pyinstaller, svglib, reportlab, Pillow, pip-audit
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump version to 1.0.0 and add --version CLI arg</name>
  <files>src/eleitorum/version.py, src/eleitorum/__main__.py, pyproject.toml</files>
  <read_first>
    - src/eleitorum/version.py (current version string to replace)
    - src/eleitorum/__main__.py (current import structure to restructure)
    - pyproject.toml (current version field and dev extras to update)
  </read_first>
  <behavior>
    - Test 1: `python -m eleitorum --version` outputs exactly "EleitorUM 1.0.0\n" to stdout and exits 0
    - Test 2: `python -m eleitorum --version` does NOT import PySide6 (subprocess check — no Qt platform plugin error on headless)
    - Test 3: importing `eleitorum.version` yields `__version__ == "1.0.0"`
  </behavior>
  <action>
    In version.py: change `__version__ = "0.1.0"` to `__version__ = "1.0.0"`. Keep the module docstring intact.

    In __main__.py: rewrite the file to the pattern from 04-PATTERNS.md §__main__.py. Add `from __future__ import annotations` at the top. Define `_check_version_flag()` function that imports `__version__` from `eleitorum.version` (no Qt), constructs an `argparse.ArgumentParser(prog="EleitorUM", add_help=False)`, adds `--version` as a `store_true` argument, calls `parse_known_args()`, and if truthy writes `f"EleitorUM {__version__}\n"` to `sys.stdout` then calls `sys.exit(0)`. Call `_check_version_flag()` at module level immediately after the function definition. Move both Qt imports (`from eleitorum.ui.app import create_app` and `from eleitorum.ui.main_window import MainWindow`) inside the `main()` function body — they must NOT appear at module level. Add `# noqa: E402` comments are not needed because imports are now inside main(). The `if __name__ == "__main__": raise SystemExit(main())` guard stays unchanged.

    In pyproject.toml: change `version = "0.1.0"` to `version = "1.0.0"`. Change the `description` field from `"Windows desktop utility to normalize electoral roll and eligibility list files for Universidade do Minho"` to `"Windows desktop utility to normalize electoral roll and eligibility list files"`. In the `[project.optional-dependencies] dev` list, append after `"pytest-qt==4.5.0"`: `"pyinstaller==6.20.0"`, `"svglib==1.6.0"`, `"reportlab==4.4.10"`, `"Pillow==12.1.1"`, `"pip-audit==2.10.0"`. Preserve all other sections (`[tool.ruff]`, `[tool.mypy]`, `[tool.pytest.ini_options]`, `[tool.coverage.*]`) exactly — do not reformat or reorder them.
  </action>
  <verify>
    <automated>python -m eleitorum --version 2>&amp;1 | python -c "import sys; out=sys.stdin.read().strip(); assert out=='EleitorUM 1.0.0', repr(out); print('PASS:', out)"</automated>
  </verify>
  <done>
    `python -m eleitorum --version` exits 0 printing "EleitorUM 1.0.0". `python -c "from eleitorum.version import __version__; assert __version__=='1.0.0'"` passes. pyproject.toml version field is "1.0.0".
  </done>
  <acceptance_criteria>
    - version.py contains exactly: `__version__ = "1.0.0"`
    - __main__.py: `_check_version_flag` function is defined and called at module level BEFORE any `from eleitorum.ui` import
    - __main__.py: `from eleitorum.ui.app import create_app` appears only inside `main()`, not at module level
    - __main__.py: `from eleitorum.ui.main_window import MainWindow` appears only inside `main()`, not at module level
    - `python -m eleitorum --version` prints "EleitorUM 1.0.0" and exits 0
    - pyproject.toml `version = "1.0.0"` (line 7)
    - pyproject.toml description contains no "Universidade" or "UMinho"
    - pyproject.toml dev extras include `"pyinstaller==6.20.0"`, `"svglib==1.6.0"`, `"reportlab==4.4.10"`, `"Pillow==12.1.1"`, `"pip-audit==2.10.0"`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove all UMinho references from strings.py and dialogs.py</name>
  <files>src/eleitorum/ui/strings.py, src/eleitorum/ui/dialogs.py</files>
  <read_first>
    - src/eleitorum/ui/strings.py (current full file — locate exact lines to modify/delete)
    - src/eleitorum/ui/dialogs.py (current full file — locate exact import and widget block)
  </read_first>
  <action>
    In strings.py:

    Line 166-168 — update ABOUT_DESCRIPTION to remove institution reference:
    change `"Utilitário para normalização de ficheiros eleitorais da Universidade do Minho."` to `"Utilitário para normalização de ficheiros eleitorais."`. Keep the surrounding constant name and type annotation unchanged.

    Lines 172-178 — delete the UMINHO_DISCLAIMER block entirely. This means removing: the comment line `# Verbatim from Eleitorum.md §3.5 — do not paraphrase.`, the `UMINHO_DISCLAIMER: str = (` line, the three body string lines, and the closing `)`. After deletion, the `# ---------------------------------------------------------------------------` separator for the About section and the `# Welcome dialog` separator below should remain, with no blank lines left hanging. Maintain existing section structure.

    Line 188 inside WELCOME_BODY — change `"eleitorais para o formato exigido pela plataforma da Universidade do Minho.\n\n"` to `"eleitorais para o formato exigido pela plataforma eleitoral.\n\n"`. The rest of WELCOME_BODY (the four numbered steps) stays unchanged.

    Update the module docstring first line (line 8): remove "The UMINHO_DISCLAIMER is verbatim from Eleitorum.md §3.5." from the docstring. The remaining docstring text about no string literals in widget code stays.

    In dialogs.py:

    Module docstring (lines 10-11): change "Contains APP_NAME + version heading, PT-PT description, UMinho disclaimer (verbatim from Eleitorum.md §3.5 via strings.UMINHO_DISCLAIMER), MIT license note, and repository link via QLabel.setOpenExternalLinks(True)." to "Contains APP_NAME + version heading, PT-PT description, MIT license note, and repository link via QLabel.setOpenExternalLinks(True).".

    Lines 36-40 — import block for strings: remove `UMINHO_DISCLAIMER,` from the multi-line import. After removal the import reads: `from eleitorum.ui.strings import (`, then `ABOUT_DESCRIPTION,`, `ABOUT_LICENSE,`, `ABOUT_REPO_LINK_LABEL,`, `BTN_COMECAR,`, `WELCOME_BODY,`, `WELCOME_HEADING,`, `)`. Keep alphabetical sort intact.

    Lines 121-125 inside AboutDialog._setup_ui() — delete the disclaimer QLabel block entirely. Remove: the comment `# UMinho disclaimer — verbatim from Eleitorum.md §3.5`, `disclaimer = QLabel(UMINHO_DISCLAIMER)`, `disclaimer.setWordWrap(True)`, `disclaimer.setObjectName("mutedText")`, `layout.addWidget(disclaimer)`. The widget sequence after deletion is: heading → description → license_lbl → repo_link → stretch → close_btn.

    Update the AboutDialog class docstring (lines 95-98): remove the `- UMinho disclaimer (verbatim UMINHO_DISCLAIMER from Eleitorum.md §3.5)` bullet; retain all other bullets.
  </action>
  <verify>
    <automated>python -c "import subprocess,sys; r=subprocess.run(['python','-c','import re,pathlib; p=[pathlib.Path(\"src\"),pathlib.Path(\"README.md\"),pathlib.Path(\"pyproject.toml\")]; hits=[(str(f),l+1,line) for root in p for f in ([root] if root.is_file() else root.rglob(\"*.py\")) for l,line in enumerate(f.read_text(encoding=\"utf-8\",errors=\"replace\").splitlines()) if re.search(r\"Universidade|UMinho\",line)]; print(hits); assert not hits'],capture_output=True,text=True); print(r.stdout,r.stderr); sys.exit(r.returncode)"</automated>
  </verify>
  <done>
    grep finds no "Universidade" or "UMinho" in src/. dialogs.py imports compile without NameError. pytest passes (existing tests that reference dialogs still green).
  </done>
  <acceptance_criteria>
    - strings.py: `UMINHO_DISCLAIMER` constant does not exist anywhere in the file
    - strings.py: `ABOUT_DESCRIPTION` value is `"Utilitário para normalização de ficheiros eleitorais."` (no institution)
    - strings.py: `WELCOME_BODY` line contains `"plataforma eleitoral"` (not "plataforma da Universidade do Minho")
    - dialogs.py: `UMINHO_DISCLAIMER` does not appear in import list or anywhere in file
    - dialogs.py: `AboutDialog._setup_ui()` contains no `disclaimer` variable or `UMINHO_DISCLAIMER` reference
    - `python -c "from eleitorum.ui.dialogs import AboutDialog"` imports without NameError
    - `grep -rn "Universidade\|UMinho" src/` returns zero lines
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add test_no_uminho_strings.py regression guard</name>
  <files>tests/unit/test_no_uminho_strings.py</files>
  <read_first>
    - tests/unit/ui/test_strings.py (test class pattern, import structure, file-content scan pattern to replicate)
    - tests/unit/ directory listing (to understand existing test structure and __init__.py presence)
  </read_first>
  <behavior>
    - Test 1: scan of clean src/ + README.md + pyproject.toml after Task 2 produces zero violations (test passes)
    - Test 2 (meta): if ABOUT_DESCRIPTION in strings.py were reverted to contain "Universidade", the test would catch it and fail with a message listing the offending file, line number, and content
  </behavior>
  <action>
    Create `tests/unit/test_no_uminho_strings.py`. Use `from __future__ import annotations` as the first non-comment line. Module docstring: "Regression test: no 'Universidade' or 'UMinho' strings remain in source or config files (D-01 — BRAND-04).\n\nScans src/, README.md, and pyproject.toml for any occurrence of institution names.\nFails if any match is found, listing every offending file, line number, and content."

    Imports (stdlib only, ruff import order): `from __future__ import annotations`, `import pathlib`, `import re`.

    Constants:
    - `_SCAN_ROOTS: list[pathlib.Path] = [pathlib.Path("src"), pathlib.Path("README.md"), pathlib.Path("pyproject.toml")]`
    - `_PATTERN: re.Pattern[str] = re.compile(r"Universidade|UMinho")`

    Class `TestNoUminhoStrings` with single method `test_no_institution_references_in_source_or_config`. Method docstring: "No file in src/, README.md, or pyproject.toml may reference institution names per D-01." Logic: iterate `_SCAN_ROOTS`; for each root, if it is a file scan that file, if it is a directory call `.rglob("*.py")`; for each file, `read_text(encoding="utf-8", errors="replace")`, enumerate lines, search with `_PATTERN`; append `f"{path}:{lineno}: {line.strip()}"` to a `violations` list on match. Final assertion: `assert not violations, "Institution references found — remove per D-01:\n" + "\n".join(violations)`.

    The test must work from any working directory where the repo root is the current directory (pytest is always run from repo root per pyproject.toml `testpaths = ["tests"]`). Use relative paths (`pathlib.Path("src")` not absolute).
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_no_uminho_strings.py -v</automated>
  </verify>
  <done>
    `pytest tests/unit/test_no_uminho_strings.py -v` reports 1 passed. Full `pytest` run remains at 384 tests (383 existing + 1 new) all green.
  </done>
  <acceptance_criteria>
    - File exists at `tests/unit/test_no_uminho_strings.py`
    - File uses `from __future__ import annotations`
    - Class is `TestNoUminhoStrings`, method is `test_no_institution_references_in_source_or_config`
    - `pytest tests/unit/test_no_uminho_strings.py -v` exits 0 with "1 passed"
    - Full `pytest -x -q` run passes (all tests including the new one)
    - `ruff check tests/unit/test_no_uminho_strings.py` reports no issues
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → argparse | `--version` flag; user controls argv but argparse accepts only known flags via parse_known_args |
| About dialog repo link | Compile-time constant in dialogs.py; never user-derived |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Spoofing | `--version` output | accept | Version read from version.py compile-time constant; no user input involved |
| T-04-01-02 | Tampering | argparse in __main__.py | mitigate | Use `parse_known_args()` so extra flags from PyInstaller wrapper do not error; no user string reaches any eval |
| T-04-01-SC | Tampering | pip install of new dev deps | mitigate | slopcheck passed for all 5 new deps (RESEARCH.md Package Legitimacy Audit); pinned exact versions in pyproject.toml |
</threat_model>

<verification>
After all three tasks complete:
- `python -m eleitorum --version` prints "EleitorUM 1.0.0" and exits 0
- `python -c "from eleitorum.version import __version__; assert __version__=='1.0.0'"` passes
- `python -c "from eleitorum.ui.dialogs import WelcomeDialog, AboutDialog"` imports cleanly (no NameError for UMINHO_DISCLAIMER)
- `pytest -x -q` passes all tests (384 total)
- `ruff check src/ tests/` reports no issues
</verification>

<success_criteria>
- version.py contains `__version__ = "1.0.0"`
- `python -m eleitorum --version` exits 0 printing "EleitorUM 1.0.0" without importing PySide6
- No "Universidade" or "UMinho" string exists in src/, README.md, or pyproject.toml
- test_no_uminho_strings.py exists and passes as part of the normal pytest suite
- pyproject.toml version is "1.0.0", description has no institution name, dev extras include the 5 new build tools
</success_criteria>

<output>
Create `.planning/phases/04-build-ci-packaging-distribution/04-01-SUMMARY.md` when done.
</output>
