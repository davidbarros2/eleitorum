---
phase: "04-build-ci-packaging-distribution"
plan: 4
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - SPECIFICATION.md
  - README.md
  - CHANGELOG.md
  - CONTRIBUTING.md
  - RENAMING.md
  - .gitignore
  - .gitattributes
autonomous: true
requirements:
  - BRAND-03
  - BRAND-04
  - REPO-01
  - REPO-02
  - REPO-03
  - REPO-04
  - REPO-05
  - REPO-06
  - REPO-07
  - REPO-08

must_haves:
  truths:
    - "SPECIFICATION.md exists at repo root and is the verbatim content of .planning/Eleitorum.md"
    - "README.md starts with a one-paragraph English About section followed by the PT-PT body, with no UMinho references"
    - "CHANGELOG.md has an empty Unreleased section and a v1.0.0 Added section listing all major capabilities"
    - "CONTRIBUTING.md states clearly that external contributions are not accepted"
    - "RENAMING.md contains a checklist of every location that references the app name EleitorUM"
    - ".gitignore covers Python, PyInstaller (dist/, build/, *.spec.bak), IDE, OS, and build artifacts"
    - ".gitattributes has text=auto baseline, *.py eol=lf, and binary markers for TTF/SVG/ICO/PNG/ZIP"
    - "LICENSE (MIT) already exists — no changes needed"
  artifacts:
    - path: "SPECIFICATION.md"
      provides: "Full project specification (verbatim from .planning/Eleitorum.md)"
    - path: "README.md"
      provides: "Bilingual README — EN paragraph at top, PT-PT body, no institution references"
    - path: "CHANGELOG.md"
      provides: "Keep-a-Changelog v1.0.0 entry with comprehensive Added section"
    - path: "CONTRIBUTING.md"
      provides: "Archived project notice — no external contributions"
    - path: "RENAMING.md"
      provides: "Checklist of all EleitorUM name locations"
    - path: ".gitignore"
      provides: "Python+PyInstaller+IDE+OS+build artifact exclusions"
    - path: ".gitattributes"
      provides: "Line ending normalization and binary markers"
  key_links:
    - from: "RENAMING.md"
      to: "src/eleitorum/config.py"
      via: "Checklist must include APP_NAME = 'EleitorUM' as the primary rename point"
      pattern: "config.py"
    - from: "README.md"
      to: "v1.0.0 GitHub Release"
      via: "Install instructions must reference the ZIP download from GitHub Releases"
      pattern: "v1.0.0"
---

<objective>
Create all repository documentation files required for public archiving: SPECIFICATION.md (verbatim spec copy), updated README.md (bilingual, no UMinho), CHANGELOG.md (v1.0.0 Keep-a-Changelog format), CONTRIBUTING.md (archived project notice), RENAMING.md (EleitorUM name checklist), .gitignore, and .gitattributes.

Purpose: These files make the repository suitable for public archiving and discovery. They are largely independent of the build tooling and can be written while the build script work proceeds in parallel.

Output: Seven files created or modified at the repo root.
</objective>

<execution_context>
@C:\Users\david\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\david\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\david\Documents\Projetos\eleitorum\.planning\ROADMAP.md
@C:\Users\david\Documents\Projetos\eleitorum\.planning\phases\04-build-ci-packaging-distribution\04-CONTEXT.md
@C:\Users\david\Documents\Projetos\eleitorum\.planning\phases\04-build-ci-packaging-distribution\04-PATTERNS.md

<interfaces>
From 04-CONTEXT.md:
  D-04: Copy .planning/Eleitorum.md verbatim to SPECIFICATION.md. Do NOT copy UMinho disclaimer from Section 3.5 — it is superseded by D-01.
  D-03: README.md — add short EN paragraph AT THE VERY TOP (before PT body); no institution named; remove any UMinho mentions.
  D-05: CHANGELOG.md in Keep-a-Changelog format; v1.0.0 Added section; empty Unreleased; no Changed/Fixed/Removed for initial release.
  CONTRIBUTING.md: Archived project, external contributions not accepted, no PRs will be reviewed.
  RENAMING.md: Checklist of every location referencing "EleitorUM" — includes config.py, pyproject.toml, README.md, SPECIFICATION.md, CHANGELOG.md, CONTRIBUTING.md, scripts/build.py, .github/workflows/*.yml, About dialog, window title, distribution ZIP filename, QSettings organization key.
  .gitignore: Python + PyInstaller (dist/, build/, *.spec.bak) + IDE (.vscode/, .idea/) + OS (.DS_Store, Thumbs.db, desktop.ini) + build artifacts (EleitorUM-*.zip, version_info.py).
  .gitattributes: * text=auto; *.py text eol=lf; *.ttf binary; *.svg binary; *.ico binary; *.png binary; *.zip binary; *.pdf binary.

From README.md (current — D-03 changes needed):
  - Add EN paragraph BEFORE line 1: "EleitorUM is a Windows desktop utility..."
  - Update badge line 8: "Em Desenvolvimento" → "v1.0.0" (brightgreen)
  - Update Phase 4 row in status table: "⏳ Pendente" → "✅ Concluída"
  - Confirm: no "Universidade" or "UMinho" in current README body (confirmed by grep in PATTERNS.md)
  - Add install instructions referencing the v1.0.0 GitHub Release ZIP

From 04-CONTEXT.md §Claude's Discretion:
  RENAMING.md must include QSettings org key "EleitorUM/EleitorUM" (company/app) from Phase 2 D-06.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SPECIFICATION.md, update README.md, create CHANGELOG.md</name>
  <files>SPECIFICATION.md, README.md, CHANGELOG.md</files>
  <read_first>
    - .planning/Eleitorum.md (full file — verbatim source for SPECIFICATION.md; identify Section 3.5 to omit)
    - README.md (current file — to prepend EN paragraph, update badge, update status table, add release install instructions)
  </read_first>
  <action>
    SPECIFICATION.md: Read `.planning/Eleitorum.md` in full. Copy its entire content verbatim to `SPECIFICATION.md` at the repo root, with one exception: locate Section 3.5 (the UMinho affiliation disclaimer paragraph — identifiable by text about "ferramenta independente" or "afiliada") and omit that specific paragraph or sub-section block, preserving the surrounding heading structure. All other content copies unchanged, including the Section 0 preamble about "audience: AI development agents and human contributors". The resulting SPECIFICATION.md must be valid Markdown.

    README.md: Prepend the following English paragraph block at the very top (before line 1, before the `# EleitorUM` heading):

    "**EleitorUM** is a Windows desktop utility that normalises electoral roll and eligibility list files. It accepts Excel and text formats (XLSX, XLS, ODS, CSV, TSV), validates and transforms the data according to strict rules, and produces a byte-exact CSV output with no manual fixing required.\n\n---\n\n"

    After prepending, update two items in the existing PT-PT body:
    (a) The status badge `[![Estado: Em Desenvolvimento](https://img.shields.io/badge/estado-em%20desenvolvimento-orange)]()` → `[![Estado: v1.0.0](https://img.shields.io/badge/estado-v1.0.0-brightgreen)]()`
    (b) The Phase 4 row in the status table: change `| 4 — Build e Distribuição | PyInstaller `.exe`, CI/CD, v1.0.0 | ⏳ Pendente |` to `| 4 — Build e Distribuição | PyInstaller `.exe`, CI/CD, v1.0.0 | ✅ Concluída |`

    Also add a new "Instalação" section in the PT-PT body, after "## Estado do Projecto" and before "## Desenvolvimento". Content:
    "## Instalação\n\nDescarregue `EleitorUM-1.0.0-win64.zip` da [página de lançamentos](https://github.com/davidbarros2/eleitorum/releases/tag/v1.0.0), extraia o ZIP e execute `EleitorUM.exe`. Não é necessário instalar Python.\n\n### Verificação de integridade (opcional)\n\n```powershell\n# Verificar SHA-256\n(Get-FileHash EleitorUM-1.0.0-win64.zip -Algorithm SHA256).Hash.ToLower()\n```\nCompare com o conteúdo de `EleitorUM-1.0.0-win64.zip.sha256`.\n\n### Aviso de segurança\n\nO Windows pode apresentar um aviso SmartScreen na primeira execução (o executável não tem assinatura de código). Clique em \"Mais informações\" → \"Executar assim mesmo\" para prosseguir.\n"

    CHANGELOG.md: Create a new file following Keep-a-Changelog 1.0.0 format:

    First line: `# Changelog`
    Blank line, then: `All notable changes to this project will be documented in this file.`
    Blank line, then: `The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).`
    Blank line, then: `## [Unreleased]`
    Blank line (empty Unreleased section — D-05 specifies empty header).
    Blank line, then: `## [1.0.0] - 2026-05-24`
    Blank line, then: `### Added`
    Blank line, then list items (one per line, each starting with `- `):
    - `- Input formats: XLSX, XLSM, XLS (xlrd), ODS (odfpy), CSV and TSV with automatic encoding detection`
    - `- Encoding detection via charset-normalizer with BOM-first fallback chain (UTF-8 BOM → UTF-8 → Windows-1252 → ISO-8859-1)`
    - `- Automatic header row detection (scores first 10 rows)`
    - `- Automatic column detection for mecanográfico and name/designation columns with synonym matching`
    - `- Manual column mapping with user-override dropdowns`
    - `- Multi-sheet Excel support: sheet picker with row counts and empty-sheet indicators`
    - `- Transformation rules: mecanográfico prefix normalisation (majority-wins case), leading-zero stripping, float-to-integer conversion, whitespace trimming and collapsing, comma removal, parenthetical annotation removal, mojibake auto-correction, U+FFFD removal`
    - `- Validation rules: prefix whitelist (A, PG, ID, F, D, B, Q, EX), positive-integer number, no duplicates within prefix, F/D/B cross-prefix collision detection, empty-name detection`
    - `- Caderno eleitoral output: UTF-8 BOM, semicolon separator, CRLF line endings, no quoting, trailing semicolon on data rows`
    - `- Lista de elegíveis output: 0-based index, alphabetical sort, same byte-exact format`
    - `- Transformation log (_LOG_) and error log (_ERRORS_) written to the output directory in PT-PT`
    - `- PySide6 wizard UI with six steps: output type, file upload (drag-and-drop), sheet picker, column mapping, preview table, result screen`
    - `- Light and dark themes with WCAG AA contrast; theme persists via QSettings`
    - `- Inter variable font bundled; background processing via QThread with progress bar and cancel support`
    - `- First-run welcome dialog; About dialog with MIT license note and repository link`
    - `- PyInstaller one-folder Windows build (EleitorUM-1.0.0-win64.zip)`
    - `- GitHub Actions CI: ruff lint, ruff format check, mypy, pytest on Python 3.11 and 3.12 (windows-latest), pip-audit CVE scanning`
    - `- GitHub Actions release workflow: build, --version smoke test, SHA-256 checksum, automatic GitHub Release publication on v1.0.0 tag`
    - `- scripts/build.py wrapper for local and CI builds`
    Blank line after the list, then:
    `[Unreleased]: https://github.com/davidbarros2/eleitorum/compare/v1.0.0...HEAD`
    `[1.0.0]: https://github.com/davidbarros2/eleitorum/releases/tag/v1.0.0`
  </action>
  <verify>
    <automated>python -c "import pathlib; f=pathlib.Path('SPECIFICATION.md'); assert f.exists() and f.stat().st_size > 10000, 'SPECIFICATION.md missing or too small'; c=f.read_text(encoding='utf-8'); assert 'Universidade do Minho' not in c or c.count('Universidade do Minho')==0, 'UMinho disclaimer still present'; print('SPECIFICATION.md OK,', f.stat().st_size, 'bytes')"</automated>
  </verify>
  <done>
    SPECIFICATION.md exists at repo root with content matching .planning/Eleitorum.md except Section 3.5 is omitted. README.md starts with the EN paragraph. CHANGELOG.md contains [1.0.0] entry with all Added items and empty [Unreleased] section.
  </done>
  <acceptance_criteria>
    - SPECIFICATION.md exists, size > 10 KB (it's a full specification document)
    - SPECIFICATION.md contains "# EleitorUM" or "EleitorUM" as a heading in Section 0
    - SPECIFICATION.md does NOT contain the UMinho disclaimer text from §3.5 (text about "ferramenta independente de código aberto" and "afiliada")
    - README.md first non-blank line (after prepend) starts with "**EleitorUM** is a Windows desktop utility"
    - README.md badge line contains "v1.0.0" and "brightgreen"
    - README.md Phase 4 row contains "✅ Concluída"
    - README.md contains "EleitorUM-1.0.0-win64.zip" in the Instalação section
    - README.md contains no "Universidade" or "UMinho" (grep confirms)
    - CHANGELOG.md first line is "# Changelog"
    - CHANGELOG.md contains "## [Unreleased]" with no items below it before the next "##"
    - CHANGELOG.md contains "## [1.0.0] - 2026-05-24"
    - CHANGELOG.md contains all 19 Added bullet items
    - CHANGELOG.md contains reference links for [Unreleased] and [1.0.0]
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create CONTRIBUTING.md, RENAMING.md, .gitignore, .gitattributes</name>
  <files>CONTRIBUTING.md, RENAMING.md, .gitignore, .gitattributes</files>
  <read_first>
    - src/eleitorum/config.py (APP_NAME constant — primary rename point for RENAMING.md)
    - .planning/phases/04-build-ci-packaging-distribution/04-CONTEXT.md (Claude's Discretion section — RENAMING.md and .gitignore/.gitattributes specifications)
  </read_first>
  <action>
    CONTRIBUTING.md: Create with the following exact content:
    "# Contributing\n\nEleitorUM is an archived project. It was built to solve a specific, bounded problem and ships as a single version (v1.0.0).\n\n**External contributions are not accepted.** Pull requests will not be reviewed or merged. Issues may be filed for documentation purposes but will not be acted upon.\n\nIf you need a similar tool adapted for a different context, please fork the repository and adapt it independently.\n\n## License\n\nAll code is released under the [MIT License](LICENSE).\n"

    RENAMING.md: Create with a comprehensive checklist of every location in the codebase and distribution that would need to change if the project name "EleitorUM" were changed to something else. Organise into sections:

    "# Renaming EleitorUM\n\nThis checklist covers every location that references the project name. Use it if the project is ever forked and renamed.\n\n## Python Source\n\n- [ ] `src/eleitorum/config.py` — `APP_NAME = \"EleitorUM\"` (primary rename point; all UI reads from here)\n- [ ] `src/eleitorum/version.py` — module docstring mentions EleitorUM\n- [ ] `src/eleitorum/__main__.py` — `argparse.ArgumentParser(prog=\"EleitorUM\")` and `--version` output string\n- [ ] `src/eleitorum/ui/strings.py` — `WINDOW_TITLE`, `WELCOME_HEADING` constants\n- [ ] `src/eleitorum/ui/dialogs.py` — window title in `setWindowTitle(f\"{APP_NAME} — Sobre\")`\n- [ ] `src/eleitorum/ui/app.py` — `app.setApplicationName(APP_NAME)`, `app.setOrganizationName(APP_NAME)`, `app.setOrganizationDomain(...)`\n\n## QSettings Registry Key\n\n- [ ] QSettings organisation key is `\"EleitorUM\"` and application key is `\"EleitorUM\"` (set in `app.py` via `setOrganizationName` / `setApplicationName`). Windows registry path: `HKCU\\Software\\EleitorUM\\EleitorUM`.\n\n## Project Metadata\n\n- [ ] `pyproject.toml` — `name = \"eleitorum\"`, `description` field, and any `[project.urls]` entries\n- [ ] `scripts/build.py` — `--name=EleitorUM`, `OriginalFilename`, `InternalName`, `ProductName`, `FileDescription` in VSVersionInfo\n- [ ] Distribution ZIP filename pattern: `EleitorUM-{version}-win64.zip` (hardcoded in `build.py` and `release.yml`)\n\n## CI/CD\n\n- [ ] `.github/workflows/ci.yml` — any `EleitorUM` references in step names or commands\n- [ ] `.github/workflows/release.yml` — ZIP filename, smoke test string `\"EleitorUM {version}\"`, release asset filenames\n\n## Repository Root Files\n\n- [ ] `README.md` — application name in title, body, and install instructions\n- [ ] `SPECIFICATION.md` — application name throughout\n- [ ] `CHANGELOG.md` — application name in entries\n- [ ] `CONTRIBUTING.md` — application name in heading\n- [ ] `RENAMING.md` (this file) — all references\n- [ ] `LICENSE` — no name reference (MIT boilerplate only)\n\n## Windows Resources\n\n- [ ] `src/eleitorum/resources/icons/EleitorUM.ico` — filename (referenced in `build.py`)\n- [ ] `src/eleitorum/resources/icons/EleitorUM-*.png` — filenames (referenced in `generate_icons.py`)\n\n## Package Directory\n\n- [ ] `src/eleitorum/` — the package directory itself; rename requires updating `pyproject.toml` `[tool.setuptools.packages.find]` and all import paths\n"

    .gitignore: Create with the following sections and entries:
    "# Python\n__pycache__/\n*.py[cod]\n*$py.class\n*.pyo\n.Python\nbuild/\ndist/\n*.egg-info/\n*.egg\n.eggs/\nenv/\nvenv/\n.venv/\n*.venv\npip-wheel-metadata/\n\n# PyInstaller\nbuild/\ndist/\n*.spec.bak\nversion_info.py\n\n# Build artifacts\nEleitorUM-*.zip\nEleitorUM-*.zip.sha256\n\n# Testing and coverage\n.pytest_cache/\n.coverage\nhtmlcov/\n*.coverage\ncoverage.xml\n\n# Mypy\n.mypy_cache/\n*.pyi.bak\n\n# IDE\n.vscode/\n.idea/\n*.sublime-project\n*.sublime-workspace\n\n# OS\n.DS_Store\nThumbs.db\ndesktop.ini\nehthumbs.db\n\n# Ruff\n.ruff_cache/\n"

    .gitattributes: Create with:
    "# Default: normalise line endings in text files on checkout\n* text=auto\n\n# Python source: always LF in working tree\n*.py text eol=lf\n\n# YAML and TOML: always LF\n*.yml text eol=lf\n*.yaml text eol=lf\n*.toml text eol=lf\n*.md text eol=lf\n*.txt text eol=lf\n\n# Binary files: no line ending conversion\n*.ttf binary\n*.otf binary\n*.woff binary\n*.woff2 binary\n*.svg binary\n*.ico binary\n*.png binary\n*.jpg binary\n*.jpeg binary\n*.gif binary\n*.zip binary\n*.pdf binary\n*.exe binary\n*.dll binary\n"
  </action>
  <verify>
    <automated>python -c "import pathlib; files=['CONTRIBUTING.md','RENAMING.md','.gitignore','.gitattributes']; missing=[f for f in files if not pathlib.Path(f).exists()]; [print('MISSING:',f) for f in missing]; assert not missing, missing; print('All 4 files present')"</automated>
  </verify>
  <done>
    All four files exist. CONTRIBUTING.md states external contributions are not accepted. RENAMING.md has the QSettings key section. .gitignore includes `version_info.py` and `EleitorUM-*.zip`. .gitattributes marks TTF/ICO/PNG/SVG/ZIP as binary.
  </done>
  <acceptance_criteria>
    - CONTRIBUTING.md exists and contains "External contributions are not accepted"
    - RENAMING.md exists and contains "QSettings" section with the registry key pattern
    - RENAMING.md contains `src/eleitorum/config.py` as the primary rename point
    - RENAMING.md contains `.github/workflows/release.yml` in the CI/CD section
    - .gitignore exists and contains `dist/`, `build/`, `*.spec.bak`, `version_info.py`, `EleitorUM-*.zip`, `.vscode/`, `.idea/`, `Thumbs.db`
    - .gitattributes exists and contains `* text=auto`, `*.py text eol=lf`, `*.ttf binary`, `*.ico binary`, `*.png binary`, `*.zip binary`
    - LICENSE already exists at repo root (no change needed — verify with: `python -c "import pathlib; assert pathlib.Path('LICENSE').exists()"`)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → GitHub Release URL | Hardcoded to the known repo URL; no user-supplied input |
| SPECIFICATION.md | Static copy of internal planning doc; no executable content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Spoofing | Repository URL in README/CONTRIBUTING | accept | URL is compile-time constant from known project metadata; no user input involved |
| T-04-04-02 | Information Disclosure | SPECIFICATION.md in public repo | accept | Spec is intentionally public; contains no personal data; AI-agent preamble is honest and appropriate |
| T-04-04-03 | Denial of Service | Missing .gitattributes binary markers | mitigate | Binary markers prevent CRLF corruption of TTF/ICO/ZIP files on Windows/Linux cross-checkout |
</threat_model>

<verification>
After all tasks:
- `python -c "import pathlib; assert all(pathlib.Path(f).exists() for f in ['SPECIFICATION.md','README.md','CHANGELOG.md','CONTRIBUTING.md','RENAMING.md','.gitignore','.gitattributes','LICENSE'])"` passes
- README.md first line contains "EleitorUM is a Windows desktop utility"
- SPECIFICATION.md size > 10 KB
- `grep -c "Universidade\|UMinho" README.md` returns 0
- CHANGELOG.md contains `## [1.0.0] - 2026-05-24`
- .gitignore contains `version_info.py` and `EleitorUM-*.zip`
- .gitattributes marks `.ttf`, `.ico`, `.png`, `.zip` as binary
</verification>

<success_criteria>
- All seven documentation and config files exist at repo root
- SPECIFICATION.md is verbatim from .planning/Eleitorum.md except Section 3.5 (UMinho disclaimer) is omitted
- README.md is bilingual (EN first paragraph, PT-PT body) with no UMinho references and includes install instructions
- CHANGELOG.md v1.0.0 entry covers all major capabilities in Added section
- CONTRIBUTING.md clearly states the archived/no-contributions policy
- RENAMING.md covers all 15+ locations including QSettings key
- .gitignore and .gitattributes are comprehensive
</success_criteria>

<output>
Create `.planning/phases/04-build-ci-packaging-distribution/04-04-SUMMARY.md` when done.
</output>
