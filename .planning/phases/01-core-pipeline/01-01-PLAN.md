---
phase: 01-core-pipeline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - .gitattributes
  - src/eleitorum/__init__.py
  - src/eleitorum/__main__.py
  - src/eleitorum/config.py
  - src/eleitorum/version.py
  - src/eleitorum/core/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/fixtures/__init__.py
  - tests/fixtures/generators.py
  - tests/unit/__init__.py
  - tests/unit/test_errors.py
  - tests/unit/test_readers.py
  - tests/unit/test_detection.py
  - tests/unit/test_transform.py
  - tests/unit/test_validate.py
  - tests/unit/test_output.py
  - tests/unit/test_logging.py
  - tests/integration/__init__.py
  - tests/integration/test_full_pipeline.py
autonomous: true
requirements:
  - INP-01
  - INP-02
  - INP-03
  - INP-04
  - INP-05
  - INP-06
  - INP-07
  - INP-08
  - INP-09
  - INP-10
  - INP-11
  - INP-12
  - INP-13
  - DET-01
  - DET-02
  - DET-03
  - DET-04
  - DET-05
  - DET-06
  - DET-07
  - TRF-01
  - TRF-02
  - TRF-03
  - TRF-04
  - TRF-05
  - TRF-06
  - TRF-07
  - TRF-08
  - TRF-09
  - TRF-10
  - TRF-11
  - TRF-12
  - TRF-13
  - TRF-14
  - TRF-15
  - VAL-01
  - VAL-02
  - VAL-03
  - VAL-04
  - VAL-05
  - VAL-06
  - VAL-07
  - VAL-08
  - VAL-09
  - OUT-01
  - OUT-02
  - OUT-03
  - OUT-04
  - OUT-05
  - OUT-06
  - OUT-07
  - OUT-08
  - OUT-09
  - OUT-10
  - OUT-11
  - OUT-12
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
  - LOG-05
  - LOG-06
  - LOG-07
  - PERF-01
  - PERF-03

must_haves:
  truths:
    - "pytest discovers the entire tests/ tree and reports the stub tests as expected-fail or skipped without import errors"
    - "ruff check . and ruff format --check . both exit 0 against the scaffold"
    - "mypy src/eleitorum exits 0 against the scaffold"
    - "python -c 'import eleitorum' succeeds from the repository root"
    - "tests/fixtures/generators.py exports all 15 fixture functions specified in Eleitorum.md Section 14.3 and they are importable without PySide6 being installed"
    - "pyproject.toml pins every dependency at the exact versions listed in RESEARCH.md Standard Stack"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, pinned dependency versions, ruff/mypy/pytest configuration"
      contains: "[tool.pytest.ini_options]"
    - path: "src/eleitorum/__init__.py"
      provides: "eleitorum package marker, exposes __version__ from version.py"
      exports: ["__version__"]
    - path: "src/eleitorum/config.py"
      provides: "APP_NAME constant per BRAND-01 contract (Phase 2 will reuse)"
      contains: "APP_NAME"
    - path: "src/eleitorum/version.py"
      provides: "__version__ string for embedding in builds and About dialog"
      contains: "__version__"
    - path: "src/eleitorum/core/__init__.py"
      provides: "core sub-package marker (modules are added in later waves)"
    - path: "tests/fixtures/generators.py"
      provides: "All 15 synthetic fixture functions per Eleitorum.md Section 14.3"
      exports:
        - "make_simple_caderno"
        - "make_simple_elegiveis"
        - "make_multi_sheet_xlsx"
        - "make_titled_xlsx"
        - "make_headerless_xlsx"
        - "make_mojibake_csv"
        - "make_whitespace_chaos_xlsx"
        - "make_with_commas"
        - "make_with_parentheses"
        - "make_duplicate_within_prefix"
        - "make_cross_prefix_collision"
        - "make_leading_zeros"
        - "make_excel_float_numbers"
        - "make_mixed_case_prefixes"
        - "make_unicode_replacement"
    - path: "tests/conftest.py"
      provides: "Shared pytest fixtures (tmp_path helpers, SYNTHETIC_NAMES, SYNTHETIC_PREFIXES constants)"
    - path: "tests/unit/test_errors.py"
      provides: "Stub test signatures for VAL-01 through VAL-09 error message coverage"
    - path: "tests/unit/test_readers.py"
      provides: "Stub test signatures for INP-01 through INP-13"
    - path: "tests/unit/test_detection.py"
      provides: "Stub test signatures for DET-01 through DET-07 and INP-07 through INP-09"
    - path: "tests/unit/test_transform.py"
      provides: "Stub test signatures for TRF-01 through TRF-15"
    - path: "tests/unit/test_validate.py"
      provides: "Stub test signatures for VAL-01 through VAL-09"
    - path: "tests/unit/test_output.py"
      provides: "Stub test signatures for OUT-01 through OUT-12"
    - path: "tests/unit/test_logging.py"
      provides: "Stub test signatures for LOG-01 through LOG-07"
    - path: "tests/integration/test_full_pipeline.py"
      provides: "Stub test signatures for end-to-end pipeline including PERF-01"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/fixtures/generators.py"
      via: "from tests.fixtures.generators import make_simple_caderno, ..."
      pattern: "from tests\\.fixtures\\.generators import"
    - from: "pyproject.toml"
      to: "src/eleitorum/"
      via: "[tool.setuptools.packages.find] where = ['src']"
      pattern: "where = \\[\"src\"\\]"
    - from: "pyproject.toml"
      to: "pytest tests/"
      via: "[tool.pytest.ini_options] testpaths = ['tests']"
      pattern: "testpaths"
---

<objective>
Stand up the entire Phase 1 project scaffold: pyproject.toml with pinned dependencies, ruff/mypy/pytest configuration, the `src/eleitorum/` package skeleton (config.py, version.py, core/__init__.py), the full `tests/` tree with all stub test files (one per core module), and the complete `tests/fixtures/generators.py` module exporting every fixture function listed in Eleitorum.md Section 14.3.

Purpose: Without this scaffold, every Wave 1 plan has nowhere to land code, no test framework to run, no fixtures to consume, and no coverage measurement target. This plan is the single dependency of every other Phase 1 plan.

Output: A runnable but empty project — `pytest tests/` collects all test files without ImportError, ruff/mypy/pytest config is valid, and all 15 fixture generators produce synthetic input files that downstream plans can consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/Eleitorum.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-pipeline/01-CONTEXT.md
@.planning/phases/01-core-pipeline/01-RESEARCH.md
@.planning/phases/01-core-pipeline/01-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Public constants and signatures that downstream Wave 1+ plans MUST be able to import. -->
<!-- This plan creates the package skeleton; the constants below are the contracts. -->

From src/eleitorum/version.py:
- `__version__: str = "0.1.0"` (development version per Eleitorum.md Section 15.5)

From src/eleitorum/config.py:
- `APP_NAME: str = "EleitorUM"` (per BRAND-01; Phase 2 wizard reads this; do not localize)

From tests/fixtures/generators.py (all 15 signatures):
- `make_simple_caderno(path: pathlib.Path) -> pathlib.Path` — clean 2-column CSV (mec + name)
- `make_simple_elegiveis(path: pathlib.Path) -> pathlib.Path` — clean 2-column CSV (designation only)
- `make_multi_sheet_xlsx(path: pathlib.Path) -> pathlib.Path` — 3 sheets: Docentes, PTAG, Alunos (Alunos has only a header row to exercise INP-11)
- `make_titled_xlsx(path: pathlib.Path) -> pathlib.Path` — row 0 is a title, headers in row 2
- `make_headerless_xlsx(path: pathlib.Path) -> pathlib.Path` — no header row; data only
- `make_mojibake_csv(path: pathlib.Path) -> pathlib.Path` — UTF-8 bytes saved as Latin-1 (e.g., `JoÃ£o`)
- `make_whitespace_chaos_xlsx(path: pathlib.Path) -> pathlib.Path` — names with tabs, NBSP (U+00A0), ZWSP (U+200B), leading/trailing spaces
- `make_with_commas(path: pathlib.Path) -> pathlib.Path` — names with trailing commas e.g. `Marta Oliveira,`
- `make_with_parentheses(path: pathlib.Path) -> pathlib.Path` — names like `Rui Pereira (Coordenador)`
- `make_duplicate_within_prefix(path: pathlib.Path) -> pathlib.Path` — same mec twice (e.g., f6688 + f6688)
- `make_cross_prefix_collision(path: pathlib.Path) -> pathlib.Path` — F500 and D500 together
- `make_leading_zeros(path: pathlib.Path) -> pathlib.Path` — mecs like F0500, D00123
- `make_excel_float_numbers(path: pathlib.Path) -> pathlib.Path` — numeric mec column stored as float (14891.0)
- `make_mixed_case_prefixes(path: pathlib.Path) -> pathlib.Path` — mix of `f6688` and `F1234`
- `make_unicode_replacement(path: pathlib.Path) -> pathlib.Path` — name containing U+FFFD
- `SYNTHETIC_NAMES: tuple[str, ...]` — module-level constant with fictional PT names (João Silva Teste, Maria Costa Exemplo, ...)
- `SYNTHETIC_PREFIXES: tuple[str, ...]` — module-level constant of valid prefixes used to seed fixtures

Pinned dependency versions for pyproject.toml (from RESEARCH.md Standard Stack — these are non-negotiable):
- python = ">=3.11,<3.14"
- openpyxl == 3.1.5
- xlrd == 2.0.2
- odfpy == 1.4.1
- pandas == 3.0.2  (RESEARCH.md A1 confirms 3.0.2 is functionally equivalent to CLAUDE.md's 3.0.3 pin)
- charset-normalizer == 3.4.7

Dev/test dependencies:
- pytest == 9.0.3
- pytest-cov == 7.1.0
- mypy == 1.19.1
- ruff == 0.15.8

Ruff config baseline (per CLAUDE.md, replaces black + isort + flake8):
- line-length = 100
- target-version = "py311"
- select = ["E", "F", "I", "B", "UP", "N", "SIM"]

Mypy config baseline (per CLAUDE.md iteration loop):
- python_version = "3.11"
- strict = false (pragmatic; pandas/openpyxl stubs are partial)
- warn_unused_ignores = true
- disallow_untyped_defs = true (for src/eleitorum/core/)

Pytest config:
- testpaths = ["tests"]
- pythonpath = ["src"]  (so `from eleitorum.core import ...` works without install)
- addopts = "-x -q --strict-markers"
- markers = ["integration: full pipeline tests (slower)", "performance: 150k-row benchmark tests"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root config files (pyproject.toml, .gitignore, .gitattributes)</name>
  <files>pyproject.toml, .gitignore, .gitattributes</files>
  <read_first>
    - CLAUDE.md (tech stack section, iteration loop, hard constraints)
    - .planning/phases/01-core-pipeline/01-RESEARCH.md (Standard Stack table for exact pinned versions, Project Constraints section)
    - .planning/Eleitorum.md (Sections 2.1, 13.1, 13.2 for layout and tool choices)
  </read_first>
  <action>
    Create pyproject.toml using PEP 621 metadata with `project.name = "eleitorum"`, `project.version = "0.1.0"` (Eleitorum.md Section 15.5: development series), `project.requires-python = ">=3.11,<3.14"`, and `project.dependencies` listing openpyxl==3.1.5, xlrd==2.0.2, odfpy==1.4.1, pandas==3.0.2, charset-normalizer==3.4.7 exactly. Add `[project.optional-dependencies].dev` with pytest==9.0.3, pytest-cov==7.1.0, mypy==1.19.1, ruff==0.15.8.

    Configure setuptools layout: `[tool.setuptools.packages.find] where = ["src"]`.

    Add `[tool.ruff]` block with line-length=100, target-version="py311", and `[tool.ruff.lint] select = ["E", "F", "I", "B", "UP", "N", "SIM"]`.

    Add `[tool.mypy]` with python_version="3.11", warn_unused_ignores=true, and a `[[tool.mypy.overrides]]` block for `module = "eleitorum.core.*"` setting `disallow_untyped_defs = true`.

    Add `[tool.pytest.ini_options]` with `testpaths = ["tests"]`, `pythonpath = ["src"]`, `addopts = "-x -q --strict-markers"`, and `markers = ["integration: full pipeline tests", "performance: 150k-row benchmark"]`.

    Add `[tool.coverage.run]` with `source = ["src/eleitorum/core"]` and `[tool.coverage.report]` with `fail_under = 90` and `show_missing = true`.

    Create .gitignore covering: __pycache__/, *.py[cod], .venv/, .pytest_cache/, .mypy_cache/, .ruff_cache/, htmlcov/, .coverage, build/, dist/, *.egg-info/, .pytest_cache, .DS_Store, Thumbs.db.

    Create .gitattributes per Eleitorum.md Section 13.1: `* text=auto eol=lf`, `*.py text eol=lf`, `*.csv text eol=crlf` (CSV outputs MUST keep CRLF on checkout), `*.png binary`, `*.ico binary`, `*.ods binary`, `*.xls binary`, `*.xlsx binary`.
  </action>
  <verify>
    <automated>python -c "import tomllib; data = tomllib.loads(open('pyproject.toml', 'rb').read().decode('utf-8')); assert data['project']['name'] == 'eleitorum'; assert data['project']['version'] == '0.1.0'; assert 'openpyxl==3.1.5' in data['project']['dependencies']; assert 'charset-normalizer==3.4.7' in data['project']['dependencies']; assert data['tool']['pytest']['ini_options']['testpaths'] == ['tests']; assert data['tool']['coverage']['report']['fail_under'] == 90; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` exists and is valid TOML (parses with `tomllib`).
    - `pyproject.toml` `[project].dependencies` lists exactly these strings (in any order): `openpyxl==3.1.5`, `xlrd==2.0.2`, `odfpy==1.4.1`, `pandas==3.0.2`, `charset-normalizer==3.4.7`.
    - `pyproject.toml` `[project.optional-dependencies].dev` lists exactly: `pytest==9.0.3`, `pytest-cov==7.1.0`, `mypy==1.19.1`, `ruff==0.15.8`.
    - `[tool.pytest.ini_options].testpaths == ["tests"]` and `pythonpath == ["src"]`.
    - `[tool.coverage.report].fail_under == 90`.
    - `[tool.ruff.lint].select` includes `"E"`, `"F"`, `"I"`, `"B"`.
    - `.gitignore` contains lines `__pycache__/`, `.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`, `htmlcov/`.
    - `.gitattributes` contains `*.csv text eol=crlf` (CSV outputs must keep CRLF semantics on any platform).
  </acceptance_criteria>
  <done>pyproject.toml, .gitignore, and .gitattributes are created at repo root with the pinned versions and config values listed above; `python -c "import tomllib; tomllib.loads(open('pyproject.toml','rb').read().decode())"` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create eleitorum package skeleton (src/eleitorum/ with config, version, core/)</name>
  <files>
    src/eleitorum/__init__.py,
    src/eleitorum/__main__.py,
    src/eleitorum/config.py,
    src/eleitorum/version.py,
    src/eleitorum/core/__init__.py
  </files>
  <read_first>
    - .planning/Eleitorum.md (Section 3.1 for APP_NAME contract; Section 13.2 for the exact layout; Section 15.5 for versioning rules)
    - .planning/phases/01-core-pipeline/01-CONTEXT.md (D-05 module structure)
    - pyproject.toml (created in Task 1 — confirm package discovery is `where = ["src"]`)
  </read_first>
  <action>
    Create `src/eleitorum/version.py` exporting `__version__ = "0.1.0"` as the only public symbol. No imports beyond stdlib (if any). Add a module docstring stating that this string is the single source of truth for build versioning per Eleitorum.md Section 15.5.

    Create `src/eleitorum/config.py` exporting `APP_NAME = "EleitorUM"` as the only public symbol (per BRAND-01; Phase 2 wizard reads this constant). Add a module docstring stating that all UI labels, window titles, log file names, and About dialog references read from this constant per Eleitorum.md Section 3.1.

    Create `src/eleitorum/__init__.py` that re-exports `__version__` from `eleitorum.version` and `APP_NAME` from `eleitorum.config`. Define `__all__ = ["__version__", "APP_NAME"]`. Do not import from `eleitorum.core` at package init (core modules will land in Wave 1 and 2; importing too eagerly would create circular-import risk).

    Create `src/eleitorum/__main__.py` containing a single stub function `def main() -> int: raise NotImplementedError("Phase 2 wires the UI entry point")` and the `if __name__ == "__main__": raise SystemExit(main())` block. This stub satisfies smoke-import requirements; Phase 2 replaces it with the QApplication launcher.

    Create `src/eleitorum/core/__init__.py` as an empty package marker with a single-line docstring `"""Qt-free core processing pipeline modules."""`. Do NOT import any submodules here — Wave 1 plans will add them.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'src'); import eleitorum; assert eleitorum.__version__ == '0.1.0'; assert eleitorum.APP_NAME == 'EleitorUM'; import eleitorum.core; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/eleitorum/version.py` contains `__version__ = "0.1.0"` exactly.
    - `src/eleitorum/config.py` contains `APP_NAME = "EleitorUM"` exactly.
    - `src/eleitorum/__init__.py` re-exports `__version__` and `APP_NAME`, defines `__all__`.
    - `src/eleitorum/core/__init__.py` exists as a valid Python package (empty body except docstring).
    - `src/eleitorum/__main__.py` defines `main()` and is importable without raising.
    - `python -c "import sys; sys.path.insert(0,'src'); import eleitorum; print(eleitorum.__version__, eleitorum.APP_NAME)"` prints `0.1.0 EleitorUM`.
    - No core submodule (`errors`, `readers`, etc.) exists yet — those land in Wave 1.
    - No PySide6 / PyQt / QtWidgets import appears anywhere under `src/eleitorum/` (verifiable with `grep -r "PySide6\|PyQt\|QtWidgets" src/eleitorum/` returning no matches).
  </acceptance_criteria>
  <done>The eleitorum package imports cleanly, exposes `__version__` and `APP_NAME`, contains an empty `core/` sub-package ready for Wave 1, and has zero Qt-related imports.</done>
</task>

<task type="auto">
  <name>Task 3: Create test infrastructure (conftest.py and all stub test files)</name>
  <files>
    tests/__init__.py,
    tests/conftest.py,
    tests/unit/__init__.py,
    tests/unit/test_errors.py,
    tests/unit/test_readers.py,
    tests/unit/test_detection.py,
    tests/unit/test_transform.py,
    tests/unit/test_validate.py,
    tests/unit/test_output.py,
    tests/unit/test_logging.py,
    tests/integration/__init__.py,
    tests/integration/test_full_pipeline.py
  </files>
  <read_first>
    - .planning/phases/01-core-pipeline/01-VALIDATION.md (Per-Task Verification Map for the exact test-function names that must exist as stubs; Wave 0 Requirements list)
    - .planning/phases/01-core-pipeline/01-RESEARCH.md (Phase Requirements → Test Map for the per-requirement test signatures)
    - .planning/Eleitorum.md (Section 14.1 testing principles: synthetic data only, ≥90% coverage on core)
    - pyproject.toml (created in Task 1 — confirm pytest markers `integration` and `performance` are registered)
  </read_first>
  <action>
    Create all `__init__.py` files as empty package markers (tests/, tests/unit/, tests/integration/).

    Create `tests/conftest.py`:
    - Import `pathlib`, `pytest`.
    - Define a module-level constant `SYNTHETIC_NAMES` — a tuple of 8–12 fictional PT names (e.g., `"João Silva Teste"`, `"Maria Costa Exemplo"`, `"Ana Pereira Sintetica"`, etc.) — used to seed fixtures. Names MUST be obviously synthetic (include the words "Teste", "Exemplo", "Sintetica" liberally) per Eleitorum.md Section 14.1.
    - Define `SYNTHETIC_PREFIXES = ("A", "PG", "ID", "F", "D", "B", "Q", "EX")` exactly matching D-08.
    - Define a `@pytest.fixture` named `tmp_csv_path` that returns a `pathlib.Path` derived from pytest's `tmp_path` with suffix `.csv`.
    - Define a `@pytest.fixture` named `tmp_xlsx_path` similar but `.xlsx`.
    - Define a `@pytest.fixture` named `synthetic_names` returning the `SYNTHETIC_NAMES` tuple.
    - Do NOT import from `eleitorum.core.*` here — those modules do not exist until Wave 1.
    - Do NOT import PySide6/PyQt — fixtures must be importable without any Qt installation per Phase 2 hook design.

    Create `tests/unit/test_errors.py` with stub test functions (each body is `pytest.skip("implemented in plan 02 — errors module")`) named exactly:
    - `test_eleitourum_error_base_class()` — base exception class exists
    - `test_mecanografico_error_message_is_pt_pt()` — PT-PT message format
    - `test_validation_error_collects_offending_rows()` — multi-row error aggregation
    - `test_encoding_detection_error_actionable_message()` — INP-08 PT-PT message
    - `test_permission_error_pt_pt_message()` — INP-13 / VAL-09 message
    - `test_unsupported_format_error_lists_accepted()` — INP-06

    Create `tests/unit/test_readers.py` with stub tests named (each `pytest.skip("implemented in plan 02 — readers module")`):
    - `test_read_xlsx_basic()` — INP-01
    - `test_read_xls_legacy()` — INP-02
    - `test_read_ods()` — INP-03
    - `test_read_csv_utf8_bom()` — INP-04, INP-07
    - `test_read_csv_utf8_no_bom()` — INP-07
    - `test_read_csv_cp1252()` — INP-07
    - `test_read_tsv()` — INP-05
    - `test_unsupported_extension_raises()` — INP-06
    - `test_permission_error_on_locked_file()` — INP-13
    - `test_xlsx_uses_read_only_and_data_only()` — PERF-03 (assert openpyxl call signature)
    - `test_multi_sheet_xlsx_returns_sheet_names_and_counts()` — INP-10
    - `test_empty_sheet_flagged()` — INP-11
    - `test_skip_trailing_empty_rows_logged_count()` — INP-12

    Create `tests/unit/test_detection.py` with stub tests (each `pytest.skip("implemented in plan 03 — detection module")`):
    - `test_detect_encoding_utf8_bom()` — INP-07
    - `test_detect_encoding_utf8_no_bom()` — INP-07
    - `test_detect_encoding_cp1252()` — INP-07
    - `test_detect_encoding_iso_8859_1_fallback()` — INP-07
    - `test_detect_encoding_undetectable_raises_pt_pt()` — INP-08
    - `test_detect_encoding_logs_choice()` — INP-09 (returns encoding so caller can log)
    - `test_header_row_scoring_picks_best_of_first_10()` — DET-01
    - `test_no_header_returns_manual_mapping_signal()` — DET-02
    - `test_mec_column_synonym_match_nfkd()` — DET-03 (must match `Nº Mec.` via NFKD)
    - `test_name_column_synonym_match_nfkd()` — DET-04
    - `test_detection_result_metadata_for_ui()` — DET-05
    - `test_ambiguous_detection_returns_all_candidates()` — DET-06
    - `test_elegiveis_hides_mec_mapping()` — DET-07 (pipeline-level kwarg behavior)
    - `test_format_fallback_regex_when_no_synonym_matches()` — D-01 hybrid detection

    Create `tests/unit/test_transform.py` with stub tests (each `pytest.skip("implemented in plan 03 — transform module")`):
    - `test_mec_whitespace_removed()` — TRF-01
    - `test_mec_float_to_int_string()` — TRF-02
    - `test_mec_leading_zeros_stripped()` — TRF-03
    - `test_prefix_case_normalization_lowercase_majority()` — TRF-04
    - `test_prefix_case_normalization_uppercase_majority()` — TRF-04
    - `test_prefix_case_normalization_tie_defaults_lowercase()` — TRF-04
    - `test_name_whitespace_strip_includes_nbsp_zwsp()` — TRF-05
    - `test_name_internal_whitespace_collapsed()` — TRF-06
    - `test_name_comma_removed()` — TRF-07
    - `test_name_parenthesis_removed_and_rewhitespaced()` — TRF-08
    - `test_mojibake_deterministic_corrected()` — TRF-09
    - `test_mojibake_ambiguous_logged_not_corrected()` — TRF-10
    - `test_replacement_char_removed_rest_preserved()` — TRF-11
    - `test_name_case_preserved()` — TRF-12
    - `test_elegiveis_sort_diacritic_stripped()` — TRF-13
    - `test_elegiveis_index_assigned_zero_based_after_sort()` — TRF-14
    - `test_caderno_preserves_input_order()` — TRF-15

    Create `tests/unit/test_validate.py` with stubs (each `pytest.skip("implemented in plan 04 — validate module")`):
    - `test_invalid_prefix_collected()` — VAL-01
    - `test_nonpositive_number_collected()` — VAL-02
    - `test_duplicate_within_prefix()` — VAL-03
    - `test_fdb_cross_prefix_collision()` — VAL-04
    - `test_fdb_cross_prefix_collision_mixed_case_input()` — VAL-04 + Pitfall 6
    - `test_a_pg_id_q_ex_independent_namespaces()` — VAL-05
    - `test_empty_name_after_transform()` — VAL-06
    - `test_caderno_requires_both_mec_and_name()` — VAL-07
    - `test_output_path_equals_input_path_refused()` — VAL-08
    - `test_permission_error_on_write_pt_pt()` — VAL-09
    - `test_all_failures_collected_before_raise()` — D-07 hard-error aggregation

    Create `tests/unit/test_output.py` with stubs (each `pytest.skip("implemented in plan 04 — output module")`):
    - `test_byte_exact_caderno_bom_crlf_no_quotes()` — OUT-01, OUT-03, OUT-04 combined
    - `test_caderno_header_exact_bytes()` — OUT-06
    - `test_caderno_row_format_third_field_empty()` — OUT-07
    - `test_elegiveis_header_exact_bytes()` — OUT-08
    - `test_elegiveis_row_format_index_zero_based()` — OUT-09
    - `test_field_separator_semicolon()` — OUT-02
    - `test_file_ends_with_crlf()` — OUT-05
    - `test_no_output_on_validation_error()` — OUT-10
    - `test_refuse_write_to_input_path()` — OUT-11
    - `test_existing_file_collision_raises_or_renames()` — OUT-12

    Create `tests/unit/test_logging.py` with stubs (each `pytest.skip("implemented in plan 04 — logging module")`):
    - `test_log_file_name_pattern()` — LOG-01
    - `test_log_encoding_utf8_with_bom()` — LOG-02
    - `test_log_line_format_timestamp_and_tag()` — LOG-02
    - `test_log_all_nine_tags_defined()` — LOG-03 (INICIO, INPUT, COLUNA, CASO, LIMPEZA, AVISO, ERRO, SAIDA, FIM)
    - `test_log_records_required_events()` — LOG-04
    - `test_error_log_file_name_pattern()` — LOG-05
    - `test_error_log_includes_row_col_value_message()` — LOG-06
    - `test_log_written_only_to_user_chosen_location()` — LOG-07

    Create `tests/integration/test_full_pipeline.py` with stubs (each `pytest.skip("implemented in plan 05 — pipeline module")`):
    - `test_happy_path_caderno_csv()` — full pipeline, byte-exact output assert
    - `test_happy_path_elegiveis_csv()` — full pipeline, byte-exact output assert
    - `test_multi_sheet_xlsx_processes_selected_sheet()` — INP-10 integration
    - `test_mojibake_file_corrected_end_to_end()` — TRF-09 integration
    - `test_duplicate_rejected_no_output_errors_log_created()` — VAL-03 + OUT-10 integration
    - One `@pytest.mark.performance` decorated stub: `test_150k_rows_under_10_seconds()` — PERF-01

    Every stub must include a `# Requirement: <ID>` comment line above its body referencing the requirement(s) it covers.
  </action>
  <verify>
    <automated>python -m pytest tests/ --collect-only -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ --collect-only -q` exits 0 (no import errors).
    - `python -m pytest tests/ --collect-only -q` reports ≥ 80 test items across all stub files (sum of the lists above is 81 stub tests + 6 integration stubs = 87; allow ≥80 for minor name variants).
    - `python -m pytest tests/unit/test_errors.py --collect-only -q` shows exactly the 6 test function names listed above.
    - `python -m pytest tests/unit/test_readers.py --collect-only -q` shows exactly the 13 test function names listed above.
    - `python -m pytest tests/unit/test_transform.py --collect-only -q` shows exactly the 17 test function names listed above.
    - `python -m pytest tests/integration/test_full_pipeline.py --collect-only -q` lists `test_happy_path_caderno_csv`, `test_happy_path_elegiveis_csv`, `test_multi_sheet_xlsx_processes_selected_sheet`, `test_mojibake_file_corrected_end_to_end`, `test_duplicate_rejected_no_output_errors_log_created`, `test_150k_rows_under_10_seconds`.
    - `tests/conftest.py` defines `SYNTHETIC_NAMES` (tuple with ≥8 entries) and `SYNTHETIC_PREFIXES = ("A", "PG", "ID", "F", "D", "B", "Q", "EX")`.
    - `grep -r "PySide6\|PyQt\|QtWidgets" tests/` returns no matches (tests must be importable without Qt).
    - Running `python -m pytest tests/ -x -q` exits 0 — every stub test is `pytest.skip(...)` so all are reported as skipped, none failed.
  </acceptance_criteria>
  <done>Pytest discovers every stub test file without import errors, all stubs are marked as skipped, conftest.py exposes the synthetic constants and shared fixtures, and the test tree is ready to be filled in by Wave 1+ plans.</done>
</task>

<task type="auto">
  <name>Task 4: Implement all 15 synthetic fixture generators in tests/fixtures/generators.py</name>
  <files>
    tests/fixtures/__init__.py,
    tests/fixtures/generators.py
  </files>
  <read_first>
    - .planning/Eleitorum.md (Section 14.3 — exact fixture function list; Section 6 — transformation rules so fixtures embed the right quirks; Section 8.1 — log format for any fixture verifying log content)
    - .planning/phases/01-core-pipeline/01-RESEARCH.md (Code Examples section for Patterns 1, 3, 5 — byte-exact CSV writing, mojibake construction, openpyxl streaming)
    - tests/conftest.py (created in Task 3 — reuse `SYNTHETIC_NAMES` and `SYNTHETIC_PREFIXES`)
  </read_first>
  <action>
    Create `tests/fixtures/__init__.py` empty package marker.

    Create `tests/fixtures/generators.py` containing all 15 fixture functions per Eleitorum.md Section 14.3. Each function takes a `pathlib.Path` argument (the target file path), writes the synthetic fixture to that path, and returns the same path.

    All functions use ONLY synthetic data — names sourced from `SYNTHETIC_NAMES` defined either at module top or re-exported from `tests.conftest`. No real personal data; all names obviously include "Teste"/"Exemplo"/"Sintetica" markers per Section 14.1.

    Implementation details per function:
    - `make_simple_caderno(path)`: write a UTF-8 BOM CSV with header `nº mec.;nome` and ~20 rows mixing prefixes (e.g., `f6688;João Silva Teste`).
    - `make_simple_elegiveis(path)`: write UTF-8 BOM CSV with header `nome` and ~20 rows of synthetic names (no mec column).
    - `make_multi_sheet_xlsx(path)`: use openpyxl Workbook with 3 sheets — "Docentes" (10 data rows), "PTAG" (5 data rows), "Alunos" (header row only, no data rows — exercises INP-11).
    - `make_titled_xlsx(path)`: openpyxl Workbook with row 0 = `["Título: Cadernos Provisórios 2026", "", ""]`, row 1 = empty, row 2 = `["Nº Mec.", "Nome", ""]`, rows 3+ = data.
    - `make_headerless_xlsx(path)`: openpyxl Workbook with NO header — first row is already data (mec value + name).
    - `make_mojibake_csv(path)`: take a UTF-8 string `"João Silva Teste"` (with combining diacritics), encode it as UTF-8, then write those bytes interpreted as Latin-1 — i.e., `s.encode("utf-8").decode("latin-1").encode("latin-1")`. The on-disk bytes contain mojibake patterns (`Ã£`, `Ã§`). Save with `.csv` extension and no BOM.
    - `make_whitespace_chaos_xlsx(path)`: openpyxl Workbook with names containing the following whitespace characters embedded as literal bytes: (a) HORIZONTAL TAB U+0009, (b) NO-BREAK SPACE U+00A0, (c) ZERO-WIDTH SPACE U+200B, (d) leading and trailing ASCII spaces U+0020. Example name (use Python escape syntax in the fixture source — these MUST be the actual code points, not literal text): `"  Jo\xe3o\u00a0Silva\tTeste\u200b  "` which is the synthetic name "João Silva Teste" wrapped with: two leading ASCII spaces, one NBSP between "João" and "Silva", one TAB between "Silva" and "Teste", one ZWSP after "Teste", two trailing ASCII spaces.
    - `make_with_commas(path)`: CSV where some names have trailing commas, e.g., `f6688;Marta Oliveira Teste,`.
    - `make_with_parentheses(path)`: CSV where some names have parenthetical annotations, e.g., `f6688;Rui Pereira Teste (Coordenador)`.
    - `make_duplicate_within_prefix(path)`: CSV containing the SAME mec (`f6688`) twice with different names (must trigger VAL-03).
    - `make_cross_prefix_collision(path)`: CSV containing both `F500` and `D500` with different names (must trigger VAL-04).
    - `make_leading_zeros(path)`: CSV with mecs `F0500`, `D00123`, `b007` to exercise TRF-03.
    - `make_excel_float_numbers(path)`: openpyxl Workbook where the mec column cell values are written as Python `float` (e.g., `ws.cell(value=14891.0)`). Use a numeric column type so openpyxl persists them as numbers.
    - `make_mixed_case_prefixes(path)`: CSV with mix of `f6688`, `F1234`, `f9001`, `F2002` so majority is lowercase (3 vs 2) and TRF-04 logs `lowercase`.
    - `make_unicode_replacement(path)`: CSV containing a name with U+FFFD: `f6688;João Silva� Teste`.

    Fixtures using openpyxl must `wb.save(path)` then close the workbook. Fixtures using CSV must use stdlib `csv` with `encoding="utf-8-sig"` (matching the OUT-01 contract) UNLESS the fixture specifically tests another encoding (mojibake fixture uses Latin-1 on-disk bytes; non-BOM happy path may omit BOM if testing INP-07 fallback).

    Add a module docstring at the top listing all 15 functions in a table, citing Eleitorum.md Section 14.3.

    Add type annotations on every function: `def make_simple_caderno(path: pathlib.Path) -> pathlib.Path:`.

    Define `SYNTHETIC_NAMES` and `SYNTHETIC_PREFIXES` at the top of generators.py (not imported from conftest, because conftest is for test fixtures via @pytest.fixture; generators.py is a library that tests call directly). The constants may be duplicated here intentionally to keep generators.py importable without pytest.

    Do NOT import PySide6/PyQt — generators must be importable without Qt installed (per Phase 3 integration test requirement and the Architecture Map "Phase 2 UI" boundary).
  </action>
  <verify>
    <automated>python -c "import sys, pathlib, tempfile; sys.path.insert(0, '.'); from tests.fixtures import generators as g; assert hasattr(g, 'make_simple_caderno'); assert hasattr(g, 'make_simple_elegiveis'); assert hasattr(g, 'make_multi_sheet_xlsx'); assert hasattr(g, 'make_titled_xlsx'); assert hasattr(g, 'make_headerless_xlsx'); assert hasattr(g, 'make_mojibake_csv'); assert hasattr(g, 'make_whitespace_chaos_xlsx'); assert hasattr(g, 'make_with_commas'); assert hasattr(g, 'make_with_parentheses'); assert hasattr(g, 'make_duplicate_within_prefix'); assert hasattr(g, 'make_cross_prefix_collision'); assert hasattr(g, 'make_leading_zeros'); assert hasattr(g, 'make_excel_float_numbers'); assert hasattr(g, 'make_mixed_case_prefixes'); assert hasattr(g, 'make_unicode_replacement'); td = pathlib.Path(tempfile.mkdtemp()); p = g.make_simple_caderno(td / 'a.csv'); assert p.exists() and p.stat().st_size > 0; p2 = g.make_mojibake_csv(td / 'b.csv'); raw = p2.read_bytes(); assert b'\xc3' in raw, 'mojibake bytes missing'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/generators.py` defines exactly these 15 function names: `make_simple_caderno`, `make_simple_elegiveis`, `make_multi_sheet_xlsx`, `make_titled_xlsx`, `make_headerless_xlsx`, `make_mojibake_csv`, `make_whitespace_chaos_xlsx`, `make_with_commas`, `make_with_parentheses`, `make_duplicate_within_prefix`, `make_cross_prefix_collision`, `make_leading_zeros`, `make_excel_float_numbers`, `make_mixed_case_prefixes`, `make_unicode_replacement`.
    - Every fixture function has signature `(path: pathlib.Path) -> pathlib.Path` and returns the same path it was given.
    - All generated files exist after invocation and have non-zero size (`p.stat().st_size > 0`).
    - `make_mojibake_csv(p).read_bytes()` contains the byte sequence `\xc3` (raw mojibake fingerprint).
    - `make_with_commas(p).read_text(encoding="utf-8-sig")` contains at least one line ending in `,` before the newline.
    - `make_with_parentheses(p).read_text(encoding="utf-8-sig")` contains the substring `(Coordenador)`.
    - `make_multi_sheet_xlsx(p)`: opening with `openpyxl.load_workbook(p, read_only=True)` shows sheet names `["Docentes", "PTAG", "Alunos"]` in that order; `Alunos` sheet has at most 1 non-empty row.
    - `make_unicode_replacement(p).read_text(encoding="utf-8-sig")` contains `�`.
    - `grep -r "PySide6\|PyQt\|QtWidgets" tests/fixtures/` returns no matches.
    - Every fixture function uses names containing one of the synthetic markers ("Teste", "Exemplo", "Sintetica"); a `grep "Silva\|Costa\|Pereira" tests/fixtures/generators.py | grep -v "Teste\|Exemplo\|Sintetica"` returns no matches (no plain real-looking name without a synthetic marker).
  </acceptance_criteria>
  <done>All 15 fixture functions exist with the exact spec names, each produces a non-empty synthetic input file embedding the quirk it is named for, no real personal data appears in the module, and the module imports without requiring Qt or any production source code.</done>
</task>

<task type="auto">
  <name>Task 5: Verify scaffold passes ruff, mypy, pytest collection, and smoke import</name>
  <files>
    (no new files; this task runs the full Phase 1 iteration loop against the scaffold from Tasks 1–4)
  </files>
  <read_first>
    - CLAUDE.md (Section 15.1 iteration loop: ruff check → ruff format --check → mypy → pytest → smoke import)
    - pyproject.toml (created in Task 1 — confirm ruff / mypy / pytest configuration is consumed)
  </read_first>
  <action>
    Install dependencies into the active environment (the environment is already provisioned per RESEARCH.md but verify): run `pip install -e ".[dev]"` from repo root. If `pip install` fails because the environment is already set up, fall back to `pip install -e .` and `pip install pytest==9.0.3 pytest-cov==7.1.0 mypy==1.19.1 ruff==0.15.8` individually.

    Run the four Phase-1 iteration-loop checks in order. Any failure must be fixed in-place before this task is considered done (do not skip):
    1. `ruff check .` — must exit 0. If formatter issues exist, run `ruff format .` to fix.
    2. `ruff format --check .` — must exit 0.
    3. `mypy src/eleitorum` — must exit 0 (the scaffold has only constants and stub modules; this should be trivial).
    4. `python -m pytest tests/ -x -q` — must exit 0 (all stub tests skip, no failures).
    5. `python -c "import sys; sys.path.insert(0, 'src'); import eleitorum; print(eleitorum.__version__, eleitorum.APP_NAME)"` must print `0.1.0 EleitorUM` and exit 0.

    If any check fails, fix the underlying issue in the appropriate file from Tasks 1–4 (e.g., add a missing type annotation, fix a ruff import-order warning) and re-run. Do not edit pyproject.toml to silence checks — fix the source.

    Final verification: produce a one-line summary string `scaffold_ready=true; tests_collected={N}; coverage_target=>=90% (will fail in wave 0 — expected)`.
  </action>
  <verify>
    <automated>ruff check . && ruff format --check . && mypy src/eleitorum && python -m pytest tests/ -x -q && python -c "import sys; sys.path.insert(0,'src'); import eleitorum; assert eleitorum.__version__=='0.1.0' and eleitorum.APP_NAME=='EleitorUM'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `ruff check .` exits 0.
    - `ruff format --check .` exits 0.
    - `mypy src/eleitorum` exits 0.
    - `python -m pytest tests/ -x -q` exits 0 with the report showing all tests as skipped (no failures, no errors).
    - `python -c "import sys; sys.path.insert(0,'src'); import eleitorum"` exits 0 and prints version + APP_NAME.
    - `grep -rn "PySide6\|PyQt\|QtWidgets\|QtCore\|QtGui" src/eleitorum/ tests/` returns no matches.
    - No core submodule (`src/eleitorum/core/errors.py` etc.) exists yet — Wave 1 will create them.
  </acceptance_criteria>
  <done>The four-step iteration loop (ruff + mypy + pytest + smoke import) is green against the Wave 0 scaffold, zero Qt imports anywhere, all 15 fixture generators produce real files, and Wave 1 plans (02 and 03) have a stable platform to land code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → eleitorum process | User-supplied input files cross this boundary; treated as untrusted until reader+validator have processed them. Wave 0 establishes the package layout; threats materialize in Waves 1–3. |
| eleitorum process → filesystem (output) | Pipeline writes CSV + log files to user-chosen destination; must never write to input path (VAL-08) and never silently overwrite (OUT-12). Wave 0 only creates infrastructure; concrete write paths land in Wave 2. |
| developer → repository | Synthetic fixture generators run as part of test suite; must never include real personal data per privacy invariant (Eleitorum.md Section 16). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-W0-01 | Information Disclosure | tests/fixtures/generators.py | mitigate | All fixture names sourced from SYNTHETIC_NAMES constant containing only fictional PT names with explicit "Teste"/"Exemplo"/"Sintetica" markers (Eleitorum.md Section 14.1). Acceptance criterion in Task 4 greps for any real-looking name lacking a synthetic marker. |
| T-1-W0-02 | Tampering | pyproject.toml dependency pins | mitigate | Every dependency pinned at exact version listed in RESEARCH.md Standard Stack (which was audited via slopcheck 0.6.1 — see RESEARCH.md Package Legitimacy Audit). No version ranges, no `>=` operators. |
| T-1-W0-SC | Tampering | pip install pytest, openpyxl, xlrd, odfpy, pandas, charset-normalizer, mypy, ruff, pytest-cov | mitigate | RESEARCH.md Package Legitimacy Audit table verified all 9 packages via slopcheck on 2026-05-23; all rated [OK]. No [ASSUMED] / [SUS] / [SLOP] packages in scope. Standard packages from the Python ecosystem with millions of downloads. No blocking human checkpoint required (slopcheck verdict is [OK] across the board). |
| T-1-W0-03 | Repudiation | Test fixture provenance | accept | Fixtures are version-controlled; provenance is the git history. No external mitigation needed for a single-developer offline project. |
</threat_model>

<verification>
After all five tasks complete:
- `ruff check . && ruff format --check . && mypy src/eleitorum && python -m pytest tests/ -x -q && python -c "import sys; sys.path.insert(0,'src'); import eleitorum"` exits 0.
- `grep -rn "PySide6\|PyQt\|QtWidgets\|QtCore\|QtGui" src/eleitorum/ tests/` returns no matches.
- All 15 fixture generators are importable and produce non-empty synthetic files.
- pytest discovers ≥80 stub tests, all skipped (none failed).
- pyproject.toml pins all dependency versions exactly per RESEARCH.md Standard Stack.
</verification>

<success_criteria>
- Wave 0 deliverables checklist from VALIDATION.md is fully ticked: pyproject.toml, all __init__.py files, conftest.py, generators.py (15 functions), 7 unit test stubs, 1 integration test stub.
- The four-step iteration loop (ruff + mypy + pytest + smoke import) is green.
- Wave 1 plans (02 and 03) can land code in `src/eleitorum/core/errors.py`, `readers.py`, `detection.py`, `transform.py` without modifying scaffold files.
- Coverage measurement is wired (`[tool.coverage.run] source = ["src/eleitorum/core"]`) and will report 0% on the empty core/ directory — this is expected for Wave 0 and will climb as Wave 1+ lands.
- The privacy invariant holds: no real personal data anywhere in the repository (acceptance criteria in Task 4 enforce this via grep gates).
</success_criteria>

<output>
Create `.planning/phases/01-core-pipeline/01-01-SUMMARY.md` when done, listing every file created, the dependency-install command used, and the iteration-loop output (ruff/mypy/pytest/smoke summaries).
</output>
