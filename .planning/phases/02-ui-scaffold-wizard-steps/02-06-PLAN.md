---
phase: 02-ui-scaffold-wizard-steps
plan: 06
type: execute
wave: 6
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
  - 02-05
files_modified:
  - src/eleitorum/__main__.py
  - src/eleitorum/ui/app.py
  - src/eleitorum/ui/wizard.py
  - src/eleitorum/ui/main_window.py
  - src/eleitorum/ui/dialogs.py
  - tests/unit/ui/test_main_window.py
  - tests/unit/ui/test_wizard.py
  - tests/unit/ui/test_dialogs.py
  - tests/unit/ui/test_app.py
autonomous: false
requirements:
  - WIZ-09
  - WIZ-10
  - APP-01
  - APP-02
  - APP-03
  - APP-04
  - APP-05
  - APP-06
  - APP-11
  - APP-12
  - APP-14
  - APP-15
  - APP-16
  - APP-17
  - APP-18
  - APP-20
  - BRAND-01

must_haves:
  truths:
    - "Running `python -m eleitorum` launches a QApplication with Fusion style, applies the saved or detected theme, and shows MainWindow"
    - "MainWindow size is 900×650 centered on first launch; restored from QSettings on subsequent launches; minimum 600×500"
    - "Menu bar contains Ficheiro (Reiniciar, Sair), Ver (Tema Claro/Escuro toggle), Ajuda (Boas-vindas, Sobre)"
    - "On first launch, welcome QDialog (modal) appears once; QSettings flag first_run_shown=True after Começar; Ajuda → Boas-vindas re-opens it"
    - "About dialog displays APP_NAME from config, __version__, UMinho disclaimer, MIT license note, repo link"
    - "Wizard navigates step_type → step_upload → (step_sheet if multi-sheet) → step_columns → step_processing → step_preview → step_done"
    - "Step indicator updates each navigation: 'Passo N de 5' on standard path, 'Passo N de 6' on multi-sheet path"
    - "Próximo button on step 3 triggers PipelineWorker dry-run (output_path=None); on success advances to step_preview; on error advances to step_done error page"
    - "Próximo (labeled 'Escolher destino e gravar') on step 4 opens QFileDialog.getSaveFileName; if path != input path triggers second worker run with confirmed output_path; on success advances to step_done success page"
    - "WIZ-10 Reiniciar resets SessionModel to a fresh instance and navigates to step_type"
    - "Theme toggle via Ver menu switches instantly without restart; persisted via QSettings"
    - "QSettings reads use type=bool / type=str discipline; window geometry/state persisted on closeEvent"
  artifacts:
    - path: "src/eleitorum/ui/app.py"
      provides: "QApplication factory: Fusion style, Inter font loading, initial theme"
      exports: ["create_app"]
    - path: "src/eleitorum/ui/wizard.py"
      provides: "WizardController managing QStackedWidget navigation, step indicator, NavBar wiring"
      exports: ["WizardController"]
    - path: "src/eleitorum/ui/main_window.py"
      provides: "MainWindow QMainWindow with menu bar, wizard host, QSettings persistence, first-run welcome"
      exports: ["MainWindow"]
    - path: "src/eleitorum/ui/dialogs.py"
      provides: "WelcomeDialog and AboutDialog QDialog subclasses"
      exports: ["WelcomeDialog", "AboutDialog"]
    - path: "src/eleitorum/__main__.py"
      provides: "Entry point: create_app() + MainWindow() + app.exec()"
      contains: "from eleitorum.ui.app import create_app"
  key_links:
    - from: "src/eleitorum/__main__.py"
      to: "src/eleitorum/ui/app.py create_app() + src/eleitorum/ui/main_window.py MainWindow"
      via: "import + instantiation"
      pattern: "from eleitorum.ui.app import create_app"
    - from: "WizardController step 3 → step 3.5"
      to: "PipelineWorker dry-run with output_path=None"
      via: "construct PipelineWorker(session.source_path, session.output_type, output_path=None); call step_processing.start_processing(worker)"
      pattern: "PipelineWorker.*output_path=None"
    - from: "WizardController step 4 → save dialog → step 3.5 (write) → step 6"
      to: "QFileDialog.getSaveFileName + second PipelineWorker call with confirmed path"
      via: "after save dialog OK, construct PipelineWorker with output_path=<chosen path>"
      pattern: "QFileDialog.getSaveFileName"
    - from: "MainWindow.closeEvent"
      to: "QSettings save geometry/state"
      via: "self._settings.setValue('window/geometry', self.saveGeometry()); self._settings.setValue('window/state', self.saveState())"
      pattern: "saveGeometry"
---

<objective>
Wire everything together: `app.py` (QApplication factory with Fusion style + Inter font + initial theme), `wizard.py` (WizardController managing step navigation, step indicator, and the two-call pipeline architecture), `main_window.py` (QMainWindow with menu bar, QSettings persistence, first-run welcome trigger), `dialogs.py` (WelcomeDialog + AboutDialog), and the new `__main__.py` entry point that ties it all together. This is the final plan of Phase 2 — when complete, `python -m eleitorum` launches a working desktop wizard.

Purpose: Convert the pile of well-tested step widgets and the QThread worker into a coherent application. This plan also handles all cross-cutting concerns: theme persistence, window geometry restoration, menu bar wiring, first-run flow, the two-call (dry-run then write) pipeline architecture, and the multi-sheet conditional navigation.

This plan ends with a `checkpoint:human-verify` because launching and walking the full wizard requires manual interaction that no automated test can fully cover (drag from real Explorer, observe UI thread responsiveness during processing, verify theme contrast).

Output: Five Python modules + four pytest-qt test files, all green; manual checkpoint pass; entry point `python -m eleitorum` produces a runnable wizard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-scaffold-wizard-steps/02-CONTEXT.md
@.planning/phases/02-ui-scaffold-wizard-steps/02-RESEARCH.md
@.planning/phases/02-ui-scaffold-wizard-steps/02-PATTERNS.md
@.planning/phases/02-ui-scaffold-wizard-steps/02-UI-SPEC.md
@CLAUDE.md
@src/eleitorum/config.py
@src/eleitorum/version.py
@src/eleitorum/__main__.py
@src/eleitorum/ui/app.py
@src/eleitorum/ui/session.py
@src/eleitorum/ui/strings.py
@src/eleitorum/ui/theme.py
@src/eleitorum/ui/worker.py
@src/eleitorum/ui/widgets/navbar.py
@src/eleitorum/ui/steps/step_type.py
@src/eleitorum/ui/steps/step_upload.py
@src/eleitorum/ui/steps/step_sheet.py
@src/eleitorum/ui/steps/step_columns.py
@src/eleitorum/ui/steps/step_processing.py
@src/eleitorum/ui/steps/step_preview.py
@src/eleitorum/ui/steps/step_done.py
@src/eleitorum/core/pipeline.py

<interfaces>
<!-- Every artifact from plans 01-05 is consumed here. Key contracts: -->

From src/eleitorum/ui/session.py:
- `SessionModel` mutable @dataclass

From src/eleitorum/ui/worker.py:
- `class PipelineWorker(QThread):` with signals `progress`, `finished`, `error`, `cancelled`
- Constructor: `(self, source, output_type, output_path, parent=None)` — `output_path=None` triggers dry-run

From src/eleitorum/ui/theme.py:
- `apply_theme(theme: str) -> None`, `detect_system_theme() -> str`, `LIGHT_QSS`, `DARK_QSS`

From src/eleitorum/ui/widgets/navbar.py:
- `class NavBar(QWidget):` with signals `anterior_clicked`, `proximo_clicked`, `cancelar_clicked`; methods `set_anterior_enabled`, `set_proximo_enabled`, `set_proximo_text`, `set_cancel_visible`

From each step:
- All step widgets accept `(session: SessionModel, parent=None)` and expose `is_complete() -> bool`
- StepUpload populates `session.source_path` and `session.sheets`
- StepProcessing exposes `start_processing(worker)`, `on_processing_started()`, `on_progress(current, total)`, and signals `route_to_preview(result)`, `route_to_error(result)`, `cancelled_by_user`
- StepPreview exposes `populate_from_session()` and `next_button_label() -> str` (returns BTN_GRAVAR)
- StepDone exposes `show_success(result)`, `show_error(result)`, signals `restart_clicked`, `quit_clicked`

From src/eleitorum/config.py:
- `APP_NAME: str = "EleitorUM"` — referenced everywhere via `from eleitorum.config import APP_NAME`

From src/eleitorum/version.py:
- `__version__: str = "0.1.0"` — referenced by app.py and About dialog
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app.py + WizardController + MainWindow + dialogs (core integration)</name>
  <files>
    src/eleitorum/ui/app.py
    src/eleitorum/ui/wizard.py
    src/eleitorum/ui/main_window.py
    src/eleitorum/ui/dialogs.py
    src/eleitorum/__main__.py
  </files>
  <read_first>
    .planning/phases/02-ui-scaffold-wizard-steps/02-PATTERNS.md (app.py, main_window.py, wizard.py sections — full templates; QSettings type= discipline; Shared Patterns: from_eleitorum.config import APP_NAME)
    .planning/phases/02-ui-scaffold-wizard-steps/02-RESEARCH.md (Pattern 1: QApplication entry point; Pattern 2: QStackedWidget navigation; Pattern 4: detect_system_theme; Pattern 5: QSettings; Pitfall 1: Fusion style; Pitfall 2: QSettings bool; Pitfall 5: multi-sheet step indicator; Pitfall 7: dry-run vs write — TWO worker calls; Code Examples: Entry Point Replacement, Centering Window, File Dialog with Last Directory, AboutDialog)
    .planning/phases/02-ui-scaffold-wizard-steps/02-UI-SPEC.md (Menu Bar table; First-Run Welcome Dialog; About Dialog; Destructive Actions — Reiniciar no confirmation; Output path conflict handling inline)
    .planning/phases/02-ui-scaffold-wizard-steps/02-CONTEXT.md (D-01 cancel returns to step 3; D-02 welcome QDialog; D-07 processing flow; specifics — Reiniciar safe; output path conflict inline)
    src/eleitorum/config.py
    src/eleitorum/version.py
    src/eleitorum/__main__.py (the current stub to replace)
  </read_first>
  <behavior>
    app.py:
    - `create_app() -> QApplication` returns a QApplication with `applicationName == APP_NAME`, `applicationVersion == __version__`, `organizationName == APP_NAME`, `style().objectName().lower() == 'fusion'`
    - Calls `_load_inter_font(app)` and `apply_theme(...)` before returning

    WizardController:
    - Constructor `WizardController(session: SessionModel, stack: QStackedWidget, navbar: NavBar, step_label: QLabel)` builds and inserts all 7 step widgets into `stack` at indices: STEP_TYPE=0, STEP_UPLOAD=1, STEP_SHEET=2, STEP_COLUMNS=3, STEP_PROCESSING=4, STEP_PREVIEW=5, STEP_DONE=6
    - Connects NavBar signals to its navigation methods
    - `_compute_next(current)` skips STEP_SHEET when `len(session.sheets) <= 1`
    - `_step_display_number()` returns `(n, 5)` standard path; `(n, 6)` multi-sheet path
    - Reiniciar navigation: replaces the session reference (or mutates fields to None) and calls `stack.setCurrentIndex(STEP_TYPE)`

    MainWindow:
    - `MainWindow()` constructs with min size (600,500), initial size (900,650) centered on primary screen
    - Menu bar contains "Ficheiro", "Ver", "Ajuda" top-level menus
    - "Ficheiro" → "Reiniciar" QAction; "Sair" QAction
    - "Ver" → "Tema Escuro"/"Tema Claro" QAction (label reflects opposite of current theme)
    - "Ajuda" → "Boas-vindas…" QAction (re-opens WelcomeDialog); "Sobre…" QAction (opens AboutDialog)
    - `closeEvent` calls `self._settings.setValue('window/geometry', self.saveGeometry())` and `self._settings.setValue('window/state', self.saveState())` before super().closeEvent
    - All QSettings reads use `type=bool` (for first_run_shown) or `type=str` (for theme, last_directory)
    - On first launch (first_run_shown == False), shows WelcomeDialog via `dialog.exec()` and sets `first_run_shown=True` afterward

    Dialogs:
    - `WelcomeDialog(parent)` is modal (`setModal(True)`); contains app name heading, PT-PT description, wizard outline, and "Começar" button that closes the dialog
    - `AboutDialog(parent)` is modal; contains app name (`APP_NAME`), version (`__version__`), UMinho disclaimer (verbatim from strings.UMINHO_DISCLAIMER), MIT license note, repo link as QLabel with `setOpenExternalLinks(True)`

    __main__.py:
    - Replaces the NotImplementedError stub with real entry point
    - `python -m eleitorum --help` is not required; raw exec is enough
    - `main()` calls `create_app()`, builds `MainWindow()`, calls `window.show()`, returns `app.exec()`
  </behavior>
  <action>
    Create `src/eleitorum/ui/app.py` with `from __future__ import annotations`, module docstring per PATTERNS.md app.py section naming APP-01, APP-07–13, BRAND-01. Implement `create_app() -> QApplication` per RESEARCH.md Pattern 1: `app = QApplication(sys.argv)`, set applicationName/organizationName/applicationVersion from config + version, call `app.setStyle('Fusion')` (critical — must precede setStyleSheet per Pitfall 1), call `_load_inter_font(app)`, read theme from QSettings using `QSettings()` directly (org/app already set on app); fall back to `detect_system_theme()` if no stored theme; call `apply_theme(theme)`. Implement `_load_inter_font(app)` per PATTERNS.md: resolve `base = pathlib.Path(getattr(sys, '_MEIPASS', str(pathlib.Path(__file__).parent.parent)))` (note: `__file__.parent.parent` because app.py lives in `src/eleitorum/ui/`, and `resources/` is in `src/eleitorum/`); glob `resources/fonts/Inter/*.ttf`; call `QFontDatabase.addApplicationFont(str(ttf))` for each; count successful loads; build a `QFont('Inter' if loaded > 0 else 'Segoe UI')` with `setPointSize(14)` and call `app.setFont(font)`. Set the window icon globally via `app.setWindowIcon(QIcon(str(base / 'resources' / 'icon.svg')))`. Create `src/eleitorum/ui/wizard.py` with `from __future__ import annotations`, module docstring per PATTERNS.md wizard.py section naming WIZ-09 and WIZ-10. Define `class WizardController(QObject):` (QObject so it owns signals); class constants `STEP_TYPE=0`, `STEP_UPLOAD=1`, `STEP_SHEET=2`, `STEP_COLUMNS=3`, `STEP_PROCESSING=4`, `STEP_PREVIEW=5`, `STEP_DONE=6`. Class-level signal `quit_requested = Signal()` (so MainWindow can react to step_done Sair). Constructor `__init__(self, session, stack, navbar, step_label, parent=None)`: instantiate all 7 step widgets (with `self._session`), insert into stack at the correct indices, store references. Connect navbar.proximo_clicked → `self.on_proximo`, navbar.anterior_clicked → `self.on_anterior`, navbar.cancelar_clicked → `self.on_cancelar`. Connect step_processing.route_to_preview → `self._on_processing_to_preview`, route_to_error → `self._on_processing_to_error`, cancelled_by_user → `self._on_processing_cancelled`. Connect step_done.restart_clicked → `self.reiniciar`, quit_clicked → `self.quit_requested`. Implement `on_proximo()`: read current index; on STEP_COLUMNS → start dry-run worker (PipelineWorker(source=session.source_path, output_type=session.output_type, output_path=None)); set stack to STEP_PROCESSING; call step_processing.start_processing(worker); on STEP_PREVIEW → open save dialog flow (see below); on STEP_TYPE/UPLOAD/SHEET → simple `_advance()` to next index (skipping STEP_SHEET if not multi-sheet). Implement save-dialog flow as a separate method `_on_preview_save_clicked()`: read last_directory from QSettings (passed via main_window or read directly); call `QFileDialog.getSaveFileName(self.stack, SAVE_DIALOG_TITLE, last_dir/suggested_name, SAVE_DIALOG_FILTER)` where suggested_name is `f"{session.source_path.stem}_{session.output_type}.csv"`; if path is empty (user cancelled), do nothing; if `pathlib.Path(path) == session.source_path`, show QMessageBox.warning with ERR_OUTPUT_SAME_AS_INPUT and re-open dialog (loop); else store `session.output_path = pathlib.Path(path)`, persist last_directory to QSettings, construct second PipelineWorker with `output_path=session.output_path`, navigate to STEP_PROCESSING, call start_processing — on success this routes to step_done success page. Implement `on_anterior()`: navigate to previous index, skipping STEP_PROCESSING (processing is never user-visible as a back target) and skipping STEP_SHEET if single-sheet path. Implement `on_cancelar()`: at STEP_PROCESSING, the cancel button on step_processing itself handles it; at other steps, treat Cancelar like "quit wizard" or just disable Cancelar on non-processing steps (per UI-SPEC, Cancelar is only on processing screen). Implement `reiniciar(self) -> None`: reset every field of `self._session` to None (mutate in place — do NOT reassign self._session, since step widgets hold the same reference); call `self._step_columns.populate_from_session` to clear visuals, etc., for every step (cheaper: navigate to STEP_TYPE and let each step's populate-on-navigate logic handle re-init when shown); set stack to STEP_TYPE. Implement `_update_navbar_for_current_step()`: read current step; set anterior_enabled (False on STEP_TYPE and STEP_PROCESSING), proximo_text (BTN_GRAVAR on STEP_PREVIEW else BTN_PROXIMO), cancel_visible (False on all steps except STEP_PROCESSING — but Cancelar on processing screen lives on the step itself, not NavBar; so NavBar's Cancelar should be hidden by default and only shown if a "Cancelar Wizard" button is required — re-check UI-SPEC: the Cancelar button in NavBar is the same Cancelar that triggers D-01 during processing; in non-processing steps it can quit the wizard. Conservative implementation: hide NavBar Cancelar on all steps EXCEPT STEP_PROCESSING; on STEP_PROCESSING, show it and route to step_processing._on_cancel_clicked). Step indicator update via `_step_display_number()` per PATTERNS.md wizard.py section. Update step_label.setText(`STEP_INDICATOR.format(n=n, total=total)`). On `_on_processing_to_preview(result)`: store `session.pipeline_result = result`; call `self._step_preview.populate_from_session()`; set stack to STEP_PREVIEW. On `_on_processing_to_error(result)`: `self._step_done.show_error(result)`; set stack to STEP_DONE. On `_on_processing_cancelled()`: per D-01, return to STEP_COLUMNS. Create `src/eleitorum/ui/main_window.py` with `from __future__ import annotations`, module docstring naming APP-01, APP-03, APP-04, APP-14. Imports per PATTERNS.md main_window.py section. Define `class MainWindow(QMainWindow):` with `__init__(self) -> None`: `super().__init__()`; `self.setWindowTitle(APP_NAME)`; `self.setWindowIcon(QIcon(str(pathlib.Path(__file__).parent.parent / 'resources' / 'icon.svg')))`; `self.setMinimumSize(600, 500)`; `self.resize(900, 650)`; center on primary screen using `screen = QApplication.primaryScreen(); fg = self.frameGeometry(); fg.moveCenter(screen.availableGeometry().center()); self.move(fg.topLeft())`. Create `self._settings = QSettings()`, `self._session = SessionModel()`. Build central widget: a vertical QWidget containing a header QLabel (step indicator), a `self._stack = QStackedWidget()`, and a `self._navbar = NavBar()`. Construct `self._wizard = WizardController(self._session, self._stack, self._navbar, self._step_label)`. Connect `self._wizard.quit_requested` to `QApplication.quit`. Build menu bar via `self.menuBar()`: Ficheiro menu (`MENU_FILE`) with Reiniciar QAction (connected to `self._wizard.reiniciar` — no confirmation per UI-SPEC Destructive Actions) and Sair QAction (connected to `self.close`). Ver menu (`MENU_VIEW`) with a Tema toggle QAction (`MENU_TEMA_ESCURO` / `MENU_TEMA_CLARO` label reflects opposite of current theme) connected to `self._on_toggle_theme`. Ajuda menu (`MENU_HELP`) with Boas-vindas QAction (`MENU_BOAS_VINDAS`) connected to `lambda: WelcomeDialog(self).exec()` and Sobre QAction (`MENU_SOBRE`) connected to `lambda: AboutDialog(self).exec()`. `_on_toggle_theme(self) -> None`: read current theme from settings (`type=str`, default 'light'), flip ('light' → 'dark' or vice versa), call `apply_theme(new)`, persist `self._settings.setValue('app/theme', new)`, update the Ver menu action's text to reflect the new opposite. `_restore_geometry(self) -> None`: `geom = self._settings.value('window/geometry')`; if geom is not None, call `self.restoreGeometry(geom)`. `_check_first_run(self) -> None`: if `not self._settings.value('app/first_run_shown', False, type=bool)`: `WelcomeDialog(self).exec(); self._settings.setValue('app/first_run_shown', True)`. Override `closeEvent(self, event)`: `self._settings.setValue('window/geometry', self.saveGeometry()); self._settings.setValue('window/state', self.saveState()); super().closeEvent(event)`. Call `_restore_geometry()` and `_check_first_run()` at end of `__init__`. Create `src/eleitorum/ui/dialogs.py` with `from __future__ import annotations`. Imports: `QDialog, QLabel, QPushButton, QVBoxLayout` from QtWidgets. Define `class WelcomeDialog(QDialog):` per UI-SPEC: setModal(True); setWindowTitle(WELCOME_HEADING); vertical layout with heading QLabel (displayHeading objectName), description QLabel (WELCOME_BODY), wizard-outline numbered list (build inline as QLabels), "Começar" QPushButton (primary objectName) connected to `self.accept`. Define `class AboutDialog(QDialog):` per UI-SPEC: setModal(True); setWindowTitle(ABOUT description heading); vertical layout with heading QLabel (`f"{APP_NAME} {__version__}"`), description QLabel (ABOUT_DESCRIPTION), UMinho disclaimer QLabel (UMINHO_DISCLAIMER, word-wrap), license QLabel (ABOUT_LICENSE — "Distribuído sob a licença MIT."), repo link QLabel with `setOpenExternalLinks(True)` and text containing the repo URL (placeholder `https://github.com/davidbarros/eleitorum` — executor should check Eleitorum.md §3 or use the actual repo URL if available; default to placeholder if unknown). Replace `src/eleitorum/__main__.py`: remove the NotImplementedError stub; new content per PATTERNS.md __main__.py section: imports `from eleitorum.ui.app import create_app` and `from eleitorum.ui.main_window import MainWindow`; `def main() -> int: app = create_app(); window = MainWindow(); window.show(); return app.exec()`; preserve `if __name__ == "__main__": raise SystemExit(main())`. Keep the module docstring discipline (update to remove the "Phase 2 wires" sentence; describe the entry point now).
  </action>
  <verify>
    <automated>python -c "from eleitorum.ui.app import create_app; from eleitorum.ui.main_window import MainWindow; from eleitorum.ui.wizard import WizardController; from eleitorum.ui.dialogs import WelcomeDialog, AboutDialog; print('all imports OK')" && python -m eleitorum --help 2>&1 | head -5 ; pytest -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/eleitorum/ui/app.py` defines `create_app() -> QApplication`; calls `app.setStyle('Fusion')` BEFORE any `setStyleSheet` (verifiable via static grep: Fusion call line < apply_theme call line in source)
    - `app.py` sets `applicationName`, `organizationName` from `APP_NAME`, `applicationVersion` from `__version__`
    - `src/eleitorum/ui/wizard.py` defines `class WizardController:` with class constants STEP_TYPE=0..STEP_DONE=6
    - WizardController.on_proximo() at STEP_COLUMNS constructs `PipelineWorker(..., output_path=None)` (dry-run); at STEP_PREVIEW opens `QFileDialog.getSaveFileName`
    - `WizardController.reiniciar()` mutates session fields in place (does NOT reassign self._session — preserves the reference shared by step widgets)
    - WizardController skips STEP_SHEET when `len(session.sheets) <= 1`
    - Step indicator returns `(n, 5)` standard / `(n, 6)` multi-sheet via `_step_display_number()`
    - `src/eleitorum/ui/main_window.py` defines `class MainWindow(QMainWindow):` with minimum size (600, 500), initial size (900, 650), centered on primary screen
    - MainWindow.closeEvent calls `saveGeometry()` and persists to QSettings before super().closeEvent
    - All QSettings.value calls in main_window.py pass `type=bool` or `type=str` (no naked calls)
    - MainWindow menu bar contains three top-level menus: Ficheiro, Ver, Ajuda — with items matching APP-14 spec
    - "Reiniciar" QAction has NO QMessageBox confirmation before calling `wizard.reiniciar()`
    - `src/eleitorum/ui/dialogs.py` defines `class WelcomeDialog(QDialog):` and `class AboutDialog(QDialog):` both with `setModal(True)`
    - AboutDialog content: APP_NAME, __version__, UMINHO_DISCLAIMER (verbatim from strings.py), MIT license note, repo link via QLabel.setOpenExternalLinks(True)
    - `src/eleitorum/__main__.py` no longer raises NotImplementedError; `main()` returns `app.exec()`
    - `python -c "from eleitorum.ui.app import create_app; from eleitorum.ui.main_window import MainWindow; from eleitorum.ui.wizard import WizardController; from eleitorum.ui.dialogs import WelcomeDialog, AboutDialog"` exits 0
    - `pytest -q` exits 0 (full suite green; new pytest-qt tests in next task also green)
  </acceptance_criteria>
  <done>app.py, wizard.py, main_window.py, dialogs.py, __main__.py all in place; imports succeed; existing tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Smoke tests for app.py, wizard navigation, main_window, dialogs</name>
  <files>
    tests/unit/ui/test_app.py
    tests/unit/ui/test_wizard.py
    tests/unit/ui/test_main_window.py
    tests/unit/ui/test_dialogs.py
  </files>
  <read_first>
    src/eleitorum/ui/app.py
    src/eleitorum/ui/wizard.py
    src/eleitorum/ui/main_window.py
    src/eleitorum/ui/dialogs.py
    src/eleitorum/config.py
    src/eleitorum/version.py
    .planning/phases/02-ui-scaffold-wizard-steps/02-VALIDATION.md (Per-Task Verification Map rows 02-xx-12, 02-xx-13)
    tests/unit/ui/conftest.py (the existing fixtures; add any new fixtures here as needed)
  </read_first>
  <behavior>
    test_app.py:
    - `test_create_app_sets_application_name_and_version`: `app = create_app(); assert app.applicationName() == 'EleitorUM' and app.applicationVersion() == '0.1.0'`
    - `test_create_app_sets_fusion_style`: `app.style().objectName().lower() == 'fusion'`
    - `test_create_app_applies_initial_stylesheet`: `app.styleSheet()` is non-empty (either LIGHT_QSS or DARK_QSS)

    test_wizard.py:
    - `test_wizard_constructs_with_seven_steps`: build WizardController; `stack.count() == 7`
    - `test_wizard_skips_sheet_step_when_single_sheet`: session.sheets = [] or [single]; navigate from STEP_UPLOAD; next index is STEP_COLUMNS (skipping STEP_SHEET)
    - `test_wizard_includes_sheet_step_when_multi_sheet`: session.sheets = [SheetInfo, SheetInfo]; navigate from STEP_UPLOAD; next index is STEP_SHEET
    - `test_wizard_step_indicator_uses_5_total_on_standard_path`: at STEP_COLUMNS with single-sheet session, `_step_display_number()` returns `(3, 5)`
    - `test_wizard_step_indicator_uses_6_total_on_multi_sheet_path`: same step but multi-sheet session, returns `(4, 6)`
    - `test_wizard_reiniciar_mutates_session_in_place`: capture `id(session)` before reiniciar; after reiniciar, `id(session)` unchanged AND `session.output_type is None` AND stack.currentIndex() == STEP_TYPE
    - `test_wizard_proximo_at_step_columns_starts_dry_run_worker` (monkeypatched): patch `PipelineWorker` constructor to record args; trigger `on_proximo` at STEP_COLUMNS; assert the worker was constructed with `output_path=None`
    - `test_wizard_save_dialog_rejects_same_as_input_path` (monkeypatched): set session.source_path; patch QFileDialog.getSaveFileName to first return the input path (rejection), then a different path; assert QMessageBox.warning was called once and getSaveFileName called twice

    test_main_window.py:
    - `test_main_window_min_size_600_500`: `window.minimumSize().width() == 600 and window.minimumSize().height() == 500`
    - `test_main_window_initial_size_900_650`: just after construction, `window.size().width() == 900 and window.size().height() == 650`
    - `test_main_window_window_title_is_app_name`: `window.windowTitle() == 'EleitorUM'`
    - `test_main_window_menu_bar_has_three_top_menus`: `[m.title() for m in window.menuBar().findChildren(QMenu)]` contains 'Ficheiro', 'Ver', 'Ajuda' (PT-PT, from strings.py)
    - `test_main_window_ficheiro_menu_has_reiniciar_and_sair`: find Ficheiro menu, enumerate its QActions, assert 'Reiniciar' and 'Sair' actions exist
    - `test_main_window_close_event_persists_geometry` (uses tmp QSettings scope via monkeypatch on QSettings or pytest-qt's `qsettings` shim): close the window; assert `QSettings().value('window/geometry')` is not None
    - `test_main_window_first_run_shows_welcome_dialog` (monkeypatch WelcomeDialog.exec to record): clear QSettings first_run_shown flag; construct MainWindow; assert exec called once
    - `test_main_window_subsequent_run_does_not_show_welcome`: set first_run_shown=True in QSettings; construct MainWindow; assert exec NOT called
    - `test_main_window_qsettings_reads_use_type_kwarg` (AST inspection): parse main_window.py source; assert every `QSettings.value(` call OR `self._settings.value(` call includes a `type=` keyword argument

    test_dialogs.py:
    - `test_welcome_dialog_is_modal`: `WelcomeDialog(None).isModal() is True`
    - `test_welcome_dialog_has_comecar_button`: find a QPushButton with text == BTN_COMECAR; clicking it calls `dialog.accept()` (verified via signal connection or by triggering and checking `dialog.result() == QDialog.Accepted`)
    - `test_about_dialog_shows_app_name_and_version`: AboutDialog children include QLabels containing both `APP_NAME` and `__version__`
    - `test_about_dialog_shows_uminho_disclaimer`: AboutDialog contains a QLabel whose text equals the UMINHO_DISCLAIMER constant from strings.py
    - `test_about_dialog_repo_link_opens_externally`: AboutDialog contains a QLabel with `openExternalLinks() == True`
    - `test_about_dialog_shows_mit_license`: AboutDialog contains a QLabel whose text contains 'MIT'
  </behavior>
  <action>
    Create `tests/unit/ui/test_app.py` with `class TestCreateApp` containing the three tests. Use pytest-qt's `qapp` fixture if needed, or construct via `create_app()` directly — note that QApplication is a singleton, so tests must use `QApplication.instance()` to retrieve the existing one. To avoid state pollution between tests, use `monkeypatch.setattr('sys.argv', ['eleitorum'])` and verify properties of `QApplication.instance()` instead of calling create_app twice. Create `tests/unit/ui/test_wizard.py` with `class TestWizardController` containing the eight tests. For the dry-run worker test, monkeypatch `eleitorum.ui.wizard.PipelineWorker` with a `MagicMock` and assert call args. For the save-dialog test, monkeypatch `QFileDialog.getSaveFileName` and `QMessageBox.warning`. Create `tests/unit/ui/test_main_window.py` with `class TestMainWindow` containing the nine tests. For QSettings isolation, use `monkeypatch.setattr('PySide6.QtCore.QSettings.setOrganizationName', ...)` OR construct each test with a fresh `QSettings` scope by monkeypatching `QSettings.value` and `QSettings.setValue` to a dict-backed shim. For the AST inspection test, use `ast.parse(open(...).read())` and walk for `Call` nodes whose `func.attr == 'value'` and verify keyword `type` is present. Create `tests/unit/ui/test_dialogs.py` with `class TestWelcomeDialog` and `class TestAboutDialog` containing the six tests. Use `qtbot.addWidget(dialog)` for cleanup. Do NOT call `dialog.exec()` in tests (blocks the test runner); use `dialog.show()` + `qtbot.waitExposed(dialog)` and inspect children via `findChildren`.
  </action>
  <verify>
    <automated>pytest tests/unit/ui/test_app.py tests/unit/ui/test_wizard.py tests/unit/ui/test_main_window.py tests/unit/ui/test_dialogs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/ui/test_app.py tests/unit/ui/test_wizard.py tests/unit/ui/test_main_window.py tests/unit/ui/test_dialogs.py -x -q` exits 0 with at least 26 tests passed (3 + 8 + 9 + 6)
    - The AST-based QSettings `type=` discipline test passes (no naked QSettings reads in main_window.py)
    - The wizard save-dialog rejection test confirms the inline-PT-PT-warning + re-open pattern (no separate step for input==output)
    - The first-run welcome test confirms QSettings flag prevents repeat display
    - `pytest -q` (full suite, Phase 1 + Phase 2 complete) exits 0
  </acceptance_criteria>
  <done>All integration smoke tests green; full Phase 2 pytest suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — full wizard walk-through, theme toggle, persistence, drag-and-drop from Windows Explorer</name>
  <files>(no files modified — manual verification only)</files>
  <read_first>
    .planning/phases/02-ui-scaffold-wizard-steps/02-CONTEXT.md
    .planning/phases/02-ui-scaffold-wizard-steps/02-VALIDATION.md (Manual-Only Verifications table)
    .planning/phases/02-ui-scaffold-wizard-steps/02-UI-SPEC.md
    src/eleitorum/ui/strings.py (UMINHO_DISCLAIMER for comparison)
  </read_first>
  <action>Pause for human verification per the procedure in <how-to-verify>. The executor must NOT modify any code in this task — it is a blocking checkpoint that gates Phase 2 completion. Wait for the user to reply with the resume-signal value. If the user reports `approved`, mark the task complete. If the user reports `issues:`, record each issue verbatim in the SUMMARY for follow-up via `/gsd-plan-phase 02 --gaps`. If `partial: blocking on <X>`, do not proceed to the SUMMARY; instead route the blocker back to the planner.</action>
  <verify>
    <human-check>User confirms PASS/FAIL for each of the ten verification sections A-J via the resume-signal mechanism. No automated command applicable.</human-check>
  </verify>
  <what-built>
    Phase 2 is structurally complete. The application can be launched via `python -m eleitorum`. Inter font files have NOT been bundled (deferred from plan 02 — `src/eleitorum/resources/fonts/Inter/` contains only OFL.txt + .gitkeep); the application falls back to Segoe UI. Bundling .ttf files is a manual download step the user must perform.

    Automated coverage for this plan:
    - 95+ pytest-qt and unit tests across plans 01-06 (all green)
    - Worker signal contract verified
    - Step widget smoke tests verified
    - Wizard navigation, multi-sheet conditional path, dry-run vs write architecture verified
    - QSettings discipline (type= kwargs) verified via AST inspection

    Remaining verifications are inherently visual / interactive and cannot be fully automated.
  </what-built>
  <how-to-verify>
    Perform each of these manual checks in order. Report results as PASS / FAIL with a one-line note.

    **A. Inter font bundling (APP-13)**
    1. Download Inter font files from https://github.com/rsms/inter/releases (latest 4.x release "Inter Variable" or "Inter Web")
    2. Extract `Inter-Regular.ttf`, `Inter-Bold.ttf`, `Inter-SemiBold.ttf` (at minimum) into `src/eleitorum/resources/fonts/Inter/`
    3. Restart the verification flow below
    4. **Pass criterion:** any letter `a` or `g` in the wizard window has Inter-style letterforms (single-story `a`, straight-leg `g`); fall back to Segoe UI looks distinctly different

    **B. First-launch flow (APP-16, WIZ-09)**
    1. Delete the QSettings store: `reg delete HKCU\Software\EleitorUM /f` (PowerShell) — if it does not exist, skip
    2. Launch: `python -m eleitorum`
    3. **Pass criteria:**
       - Welcome dialog appears modally
       - "Começar" closes it
       - Step 1 is visible
       - Anterior button is disabled
       - Step indicator reads "Passo 1 de 5"

    **C. Theme toggle (APP-11, APP-12)**
    1. From step 1, click Ver → Tema Escuro
    2. **Pass criteria:**
       - Instant switch (no flicker, no restart)
       - All text readable in dark mode
       - Menu item label now reads "Tema Claro"
    3. Close and relaunch
    4. **Pass criterion:** dark theme restored from QSettings

    **D. Full wizard walk-through (WIZ-01..11)**
    1. Use a synthetic XLSX or CSV file — generate one via:
       ```python
       import openpyxl
       wb = openpyxl.Workbook(); ws = wb.active
       ws.append(["No Mec.", "Nome"])
       for i in range(1, 21): ws.append([f"f{i}", f"Sintetico Teste {i}"])
       wb.save("teste.xlsx")
       ```
    2. Step 1: Select "Caderno Eleitoral" — verify second card visually deselects; Próximo enables
    3. Step 2: **Drag teste.xlsx from Windows Explorer onto the drop zone.** Verify border changes to accent red on drag-enter; resets on drop. File name appears below drop zone.
    4. Step 2 (alt): Click "Escolher ficheiro…" — verify native QFileDialog opens; pick teste.xlsx; same outcome
    5. Step 2.5: Skipped because XLSX has single sheet — verify wizard jumps directly to step 3
    6. Step 3: Column mapping shows pre-populated values (mecanográfico="No Mec.", nome="Nome"). Click "Alterar" to verify dropdown opens.
    7. Step 3 → Próximo: progress screen appears. **While processing, drag the window with the mouse — it must move freely (PERF-02).** Cancel button visible.
    8. Step 4: Preview table shows up to 50 rows. Summary shows "Total de linhas: 20" and "Alterações aplicadas: N". Click "Ver detalhes" — log expands. Click again — collapses.
    9. Step 4 → "Escolher destino e gravar": Save dialog opens. Pick a destination (NOT the input path). After confirming, processing screen reappears briefly, then step 6 success.
    10. **Pass criterion for VAL-08:** select the SAME path as input → inline PT-PT warning appears; save dialog re-opens. Then select different path; succeeds.
    11. Step 6 success: "Pronto!" heading visible. Click "Abrir pasta" — Windows Explorer opens at the output directory. Click "Processar outro ficheiro" — returns to step 1 with fresh session.

    **E. Error path (WIZ-08)**
    1. Build a synthetic XLSX with a duplicate mecanográfico (two rows with `F100`)
    2. Walk through wizard; processing should fail validation
    3. **Pass criteria:**
       - Step 6 error page appears
       - "Erro no processamento" heading
       - First N errors listed in QTextEdit
       - "Abrir pasta" opens the directory containing `_ERRORS_` log
       - NO Sair button on error page
       - "Processar outro ficheiro" works

    **F. Cancel during processing (D-01, WIZ-11)**
    1. With a large file (use `tests/fixtures/generators.py` huge_caderno_xlsx_path or build a 150k-row file), start processing
    2. Click Cancelar during the determinate-bar phase
    3. **Pass criteria:**
       - QMessageBox appears with "Tem a certeza que quer cancelar? O processamento será interrompido."
       - Choosing "Não, continuar" dismisses dialog; processing resumes
       - Choosing "Sim, cancelar" returns wizard to step 3 (column mapping); NO output file written

    **G. WCAG AA contrast spot-check (APP-09)**
    1. In light theme: open any step. Use a screen contrast checker (e.g., WebAIM contrast checker — manually copy hex from theme.py) and verify body text (#1A1A1A on #FAFAFA) ratio ≥ 4.5:1.
    2. Repeat for dark theme: #F5F5F5 on #1A1A1A.
    3. **Pass criterion:** both pass WCAG AA for normal text (≥4.5:1)

    **H. Window geometry persistence (APP-04)**
    1. Resize the window to a non-default size, move it to a non-default position
    2. Close the application
    3. Relaunch
    4. **Pass criterion:** window opens at the same size and position

    **I. Reiniciar (WIZ-10)**
    1. Walk to step 3
    2. Ficheiro → Reiniciar
    3. **Pass criteria:** wizard returns to step 1 immediately (no confirmation dialog); all session state cleared

    **J. About dialog (APP-15, BRAND-04)**
    1. Ajuda → Sobre…
    2. **Pass criteria:**
       - "EleitorUM 0.1.0" heading
       - PT-PT description
       - UMinho disclaimer verbatim (compare against `UMINHO_DISCLAIMER` constant in strings.py; flag if Eleitorum.md §3.5 has different wording)
       - "Distribuído sob a licença MIT."
       - Repository link is clickable (opens default browser)
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved` — all manual verifications pass
    - `issues: <description>` — list any failed checks (e.g., "F: dialog not dismissable; H: position lost"); we will create gap-closure plans
    - `partial: blocking on <X>` — indicate any blocker requiring code changes before proceeding (e.g., "blocking on Inter font not bundled")
  </resume-signal>
  <acceptance_criteria>
    - User has performed all ten verifications A-J
    - User has reported `approved` OR `issues:` with specific failures
    - Any reported issues are tracked as gap-closure items for a `--gaps` plan if they fall within Phase 2 scope (i.e., not Phase 3 integration/testing or Phase 4 build/packaging)
  </acceptance_criteria>
  <done>Manual verification complete; pass/fail recorded; gap list (if any) captured for next phase or `--gaps` mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User file dialog → wizard | Path strings from QFileDialog; checked for VAL-08 (output != input) inline |
| QSettings registry → MainWindow | Stored geometry/theme/flags; deserialized with `type=` discipline (Pitfall 2 mitigated) |
| About/Welcome dialogs → external browser | Repository link via setOpenExternalLinks; Qt sanitizes |
| Worker thread → step_processing → wizard | Cross-thread signal handoff via Qt queued connections (thread-safe) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Tampering | Wizard save dialog returns input path (VAL-08) | mitigate | Inline check: `if pathlib.Path(chosen) == session.source_path` → QMessageBox.warning(ERR_OUTPUT_SAME_AS_INPUT) and re-open dialog; do NOT advance to write |
| T-02-06-02 | Tampering | QSettings tampered (registry edited externally) to set first_run_shown=invalid | mitigate | `type=bool` coercion; any non-bool value falls back to default (False) per Qt semantics |
| T-02-06-03 | Information Disclosure | Window title or About dialog leaks user data | accept | Window title is `APP_NAME` constant; About dialog content is static + UMINHO_DISCLAIMER + version; no user input shown |
| T-02-06-04 | Denial of Service | First-run welcome dialog blocks main window indefinitely | mitigate | `setModal(True)` blocks input but does not deadlock; "Começar" button is always present and connected to dialog.accept |
| T-02-06-05 | Tampering | Cancel during write phase leaves partial output file | mitigate | Architecture per D-07 / Pitfall 7: dry-run (output_path=None) NEVER writes; the write call only runs AFTER the save dialog confirms. Cancel during write phase is rare (write is fast post-validation) but if it occurs, the open Python `csv.writer` context closes the partial file; Phase 1 `output.py` writes atomically per OUT-10 invariant (never partial on validation failure) — but a mid-CSV-write cancel could leave a partial file. **Accepted risk for v1.0.0** — cancel-during-write is an edge case (typically <1s); user can re-run |
| T-02-06-06 | Tampering | Repository link in About dialog opens malicious URL if constant is tampered | accept | URL is hardcoded in strings.py / dialogs.py source; tamper requires code edit; standard QLabel.openExternalLinks behavior, no shell injection |
| T-02-06-SC | Tampering | pip install of PySide6/pytest-qt at build time | mitigate | Plan 01 already enforced slopcheck `[OK]` and version pinning; no new installs in this plan |
</threat_model>

<verification>
- `pytest -q` (full suite — Phase 1 + Phase 2 complete) exits 0 with all tests passing
- `python -c "from eleitorum.ui.app import create_app; from eleitorum.ui.main_window import MainWindow; from eleitorum.ui.wizard import WizardController; from eleitorum.ui.dialogs import WelcomeDialog, AboutDialog; from eleitorum.__main__ import main; print('integration imports OK')"` exits 0
- `grep -nE 'self\._settings\.value' src/eleitorum/ui/main_window.py | grep -v 'type='` returns NO matches (all QSettings reads must use type= kwarg)
- `grep -nE "app\.setStyle\(['\"]Fusion['\"]\)" src/eleitorum/ui/app.py` returns one match
- Manual checkpoint (Task 3) returns `approved` or `issues:` from user
</verification>

<success_criteria>
- `python -m eleitorum` launches a wizard window with menu bar, branded icon, themed UI
- WizardController correctly routes through all 7 steps (incl. multi-sheet conditional)
- Two-call pipeline architecture: dry-run on STEP_COLUMNS → STEP_PROCESSING → STEP_PREVIEW; write call on STEP_PREVIEW save dialog → STEP_PROCESSING → STEP_DONE
- Theme toggle, geometry persistence, first-run welcome, Reiniciar, About dialog all functional
- All Phase 2 requirements satisfied: WIZ-01..11, APP-01..20, BRAND-01..02, TST-10, PERF-02 (modulo manual-only verifications listed in 02-VALIDATION.md)
- Manual checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/02-ui-scaffold-wizard-steps/02-06-SUMMARY.md` when done. Include:
- Full Phase 2 test count and pass status
- The manual checkpoint outcome (`approved` / `issues: ...`) and any blockers carried forward
- Any TODO markers left in code that the user should be aware of (e.g., UMinho disclaimer verbatim verification against Eleitorum.md §3.5)
- Inter font bundling status (manual download instructions completed Y/N)
- A final note on Phase 2 readiness for Phase 3 (integration tests, byte-exact assertions)
</output>
